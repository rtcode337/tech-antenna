@* 一覧のページに置く案内。**ジョブのボタンは設定に集めてある**ので、
   「集めなおしたいときはどこへ行けばいいか」を読む場所に必ず示す ——
   一覧の画面にボタンを置くと、読みに来た人が押す場所と集め直す人が探す場所が混ざる *@
<p class="collection-hint text-muted">
    ここに出るのは<strong>集めて保存したもの</strong>です(収集は自動では走りません)。
    <a href="/settings/general">設定</a>の「@Job」で集めなおせます。
</p>

@code {
    /// <summary>設定のどのボタンで集めなおすか(JobRunner の Name に合わせる)。</summary>
    [Parameter]
    public string Job { get; set; } = "トレンドの収集";
}
