# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このリポジトリの目的

技術情報を自動収集し、**記事・ニュース・論文・イベント・書籍を1つの導線にまとめて提示する**Webアプリ。
「今このトピックが伸びている → 関連する勉強会が近くである → 深掘りするならこの本・この論文」をつなぐ。

## 技術スタック

| 領域 | 採用 |
|---|---|
| ランタイム | .NET 10 (LTS, 2028-11 まで) |
| Web | ASP.NET Core + Blazor |
| 収集ジョブ | `BackgroundService` |
| DB | PostgreSQL + EF Core |
| LLM 要約 | Chiezo 経由(相手を画面で選ぶ)/ Claude Code(CLI ブリッジ経由)/ Anthropic API |

.NET 9 (STS) と .NET 8 (LTS) はどちらも 2026-11 に EOL のため採用しない。

## 開発環境

**ホストに .NET SDK を直接入れて開発する**(devcontainer は使わない)。Ubuntu 26.04 では
公式アーカイブに .NET 10 があるので、Microsoft のフィードを足さずに入る:

```
sudo apt install -y dotnet-sdk-10.0
```

SDK が無いホストでも、`Dockerfile` と同じ `mcr.microsoft.com/dotnet/sdk:10.0` で
ビルドやテストは通せる:

```
docker run --rm -v "$PWD":/src -w /src -e HOME=/tmp mcr.microsoft.com/dotnet/sdk:10.0 dotnet test
```

## 定期実行と手動実行

**すべてのジョブは画面のボタンから手動で実行できる**(置き場は `/settings/general`。
「収集のボタンは設定に集める」を参照)。それに加えて、**時刻を決めて自動で走らせられる** ——
**チェックは各ボタンのすぐ左**にあり(別の場所に一覧を作ると、どのボタンの設定なのかを
名前で照合することになる)、**実行する時刻**(何個でも。日本時間)はジョブの一覧の下で
まとめて設定する。**既定はチェックなし・時刻なし = 走らない** ——
消し忘れたサーバーが収集先を叩き続けたり、LLM や外部 API の無料枠を使い切らないため。

設定は API キーと同じく DB(`Secrets`。`ScheduleSettings` の `Schedule:Times` と
`Schedule:Job:<キー>`)に持ち、**環境変数では設定できない**。ワーカーは常に登録され、
周回(1分ごと)のたびに設定を読むので、**切り替えは再起動なしで効く**。

**時刻になったら、チェックの入ったジョブが決まった順で通しで1回ずつ走る**
(`Services/ScheduledJobs.InOrder`。**各行のチェックの横に出る番号がこの順番**)。
中身は `Services/ScheduleRunner`(他と同じ `JobRunner`)で、**時刻で走るのも画面の
「定期実行を今すぐ実行」も同じここを通る** —— ワーカーは時刻の判定だけを持つ:

| 順 | ジョブ | なぜこの位置か |
|---|---|---|
| 1 | トレンド / 書籍 / イベント / 論文 / 定番 の収集 | 材料そのものを増やす。**収集どうしは互いに依存しない**ので、並びはサイドバーに合わせてある |
| 2 | 話題度を取り直す | 集まった記事のタグを観測し、**次の仕分けの対象を決める** |
| 3 | タグを仕分けなおす | そこで溜まった語を LLM が語彙へ入れる |
| 4 | 記事の要約 / 論文タイトルの翻訳 | 集まった記事に要約と訳題を付ける |
| 5 | 今日のサマリーの生成 | **要約と訳題まで揃ったもの**を材料にまとめる |

**順番が要点。** かつてはジョブごとに `BackgroundService` を持ち、それぞれ「N 分ごと」で
回していた(`AutoRunWorker`)。互いの結果を待たないので、サマリーがその日の収集を含まない
材料で作られることがあった。いまはワーカー1本(`Workers/ScheduleWorker`)が順に await する。

- **前回の実行時刻は DB に持つ**(`Schedule:LastRunAt`)。メモリだと再起動のたびに
  「時刻を跨いだか」が分からなくなり、起動のたびに走ってしまう
- **止まっていたあいだに跨いでいたら、起動後の最初の周回で1回だけ走る。**
  何回跨いでいても1回 —— まとめて取り返しても集まるものは同じで、外部を余計に叩くだけ
- **印(`LastRunAt`)はジョブを始める前に進める。** 列は数十分かかることがあり、その間の
  周回でもう一度「跨いだ」と判定されると二重に走り出す
- **時刻を初めて設定したときは走らせず、そのときを起点にする** ——
  走らせると「今日のぶんはもう過ぎている」時刻の設定が、保存した瞬間に発火する。
  時刻を空にしたときは `LastRunAt` も消す(設定しなおした直後の発火を防ぐ)
- **未設定(LLM のキーが無い等)のジョブは飛ばして次へ進む。** チェックは外さない ——
  キーを入れた次の回から動き出す。**1つの失敗で残りを止めない**(収集元が1つ落ちている
  だけでサマリーまで止まると、その日のまとめが丸ごと無くなる)
- **「定期実行を今すぐ実行」は印(`LastRunAt`)を動かさない** —— 手で押したことで
  その日の時刻の回が消えると、「試しに押したら今日は走らなくなった」になる。
  進捗は列の中のどこかを出す(「3/5 話題度を取り直す を実行中…」)

実行の中身は `BackgroundService` ではなく **`JobRunner` の派生クラス**(`Services/`)に
あり、**定期実行と画面のボタンが同じ経路を通る**。`JobRunner` が `SemaphoreSlim` で
直列化するので、**自動実行中にボタンを押しても二重には走らない** —— 二重に走ると同じ
収集先へ続けて叩きに行ったり、同じ記事を二度要約して LLM の枠を無駄にする。
定期実行は `RunAndRecordAsync`(await する版の `StartInBackground`)を使うので、
**結果の文言は手動で押したときと同じように画面に残る**。

サイドバーは**同じ高さに5本**。ホーム(入口)のあと、**3つの軸**が並び、最後が設定:

| 軸 | まとめ(グループの先頭) | 下に付くもの | 何で集めるか |
|---|---|---|---|
| 外で何が話題か | `/recent/general` トレンド | ニュース / 記事 / **書籍**(本になっているテーマ)/ 論文 | RSS の巡回と HF Daily Papers、NDL サーチの新刊(**どれも選択に依存しない**) |
| 自分の興味を深掘る | `/interests/general` 興味トピック | イベント / 書籍 / 論文 | **選んだトピックを検索語**にした検索(一覧も選んだトピック+配下だけ) |
| 薦められ続けるもの | `/classics/general` 定番 | イベント(`/classics/events`)/ 書籍(`/classics/books`) | 書籍は「読むべき技術書」記事の推薦、イベントは**集めたものから注目度で選ぶ**(**どちらも選択に依存しない**) |

**URL は軸の階層をそのまま写す**(`/recent/news`・`/interests/events`・`/settings/tags`)。
かつては子ページを平らに置いていた(`/news`・`/events`・`/tags`)が、**どの軸の下にある
ページなのかが URL から読めなかった** —— 同じ「論文」が2つある(`/recent/papers` =
HF Daily Papers、`/interests/papers` = トピック検索の論文)ことも、同じ「イベント」が2つある
(`/interests/events` = 選んだトピック、`/classics/events` = 選択に依らない注目度)ことも、
階層があって初めて名前で表せる。
**見出しにも親を「/」で足す**(`トレンド / 論文`・`興味トピック / 論文`)—— 軸で2つある名前は、
ページを開いただけではどちらか読めない。子ページの見出しはすべてこの形にそろえてある。

- **メニューの親の行はページではない**(押すと子が開くだけ)。行き先が1つに決まらない
  ものをリンクにすると、押した人は「子を見たい」のに畳まれたまま遷移してしまう。
  親が持っていた一覧は各グループの**「まとめ」**へ移してある
  (`/recent/general`・`/interests/general`・`/classics/general`)。**設定だけは「全般」**——
  あそこはジョブのボタンの置き場で、内容のまとめではない。
  **URL は `general` のまま**(表示名を変えただけでブックマークを切らさない)
- **軸の入口だった 4 つの URL だけはリダイレクトを置く**(`SectionRedirect`)——
  ブックマーク・通知・外から貼られたリンクが 404 になるため。`replace: true` で送るので、
  戻るボタンで転送元へ戻って往復することはない。**子ページの旧 URL(`/news` など)は
  今も 404 のまま** —— 個人運用でブックマークの数も知れているので、経路を2つ抱えない
- **`ArticleListPage` はパス全体で種別を見分ける**(末尾だけでは `papers` が2つあって
  決まらない)。サイドバーの「開いているグループ」の判定は親のパス1つで足りるようになった
  (子ページの名前を並べた配列は消してある)
- **内部名は変えていない**(`IntegrationAxis.Trending` など)—— 画面の呼称は
  「トレンド」(かつては「直近動向」)だが、表示名の変更でコードの識別子まで巻き込まない。
  話題度の材料の「外部トレンド」(Qiita・はてブ)とは別の話

**この軸の分け方が画面構成の背骨。** 同じ「論文」でも探し方の軸が違うので別のページに置く
(`ArticleKind.TrendingPaper` と `ArticleKind.Paper`)。同じ「書籍」も軸で分かれる
(選んだトピックの検索が `/interests/books`、トピックに寄らない推薦が `/classics/books`)。
**同じ「イベント」も軸で分かれる**が、こちらは**保存されたデータは1つで、絞り方だけが違う**
(`/interests/events` = 選んだトピック+配下、`/classics/events` = 絞らずに注目度で切る)——
論文・書籍と違って収集の起こし方が同じなので、種別の列も専用のジョブも増やしていない。
**軸はホームの子ではなく同列に置く** —— 子に入れると、対等な入口だと見えない。設定の下はトピック/主催者/外部連携で、
トピックの下にさらにタグが1段下がる(トピックの詳細 `/settings/topics/<タグ>` も設定の側)。
折りたたみは `details`/`summary` で、**開いているページのグループを既定で開く**
(`NavigationManager` で現在のパスを見る)。全ページ静的 SSR なので JS は使わない。

- **開いているグループ以外も DOM に残して三角で開けるようにする。** 現在地のグループだけ
  出すと、設定の下にいるあいだ記事や書籍へ辿れなくなる
- **開閉の三角は標準マーカー(`::marker`)ではなく自前の `.caret` で描く**(トピックの
  ツリーと同じ流儀)。マーカーの実寸はブラウザ任せで、三角を持たないホームの行と
  字下げを合わせられないため。**消したままにはしないこと** —— 閉じたグループを開く
  手がかりが無くなる
- **ホームだけ `details` ではない**(子が無い)。同じ高さに並んでいると見えるよう、
  `summary` と同じ flex の組み方にして、三角の代わりに**同じ幅の `.caret-spacer`**を
  入れる —— 字下げを CSS の数値で合わせようとすると三角の実寸に依存して必ずずれる。
  先頭の余白も `.nav-root` 側に付ける(`.nav-group:first-of-type` に付けると
  ホームとトレンドのあいだだけ空く)
- 興味トピックのアイコンは**★**。カードで「興味のあるトピックに当たる記事」に付く★と
  同じ記号にして、どの軸の話かが結びつくようにしている
- **スマホ幅ではロゴの帯全体がメニューの開閉を兼ねる**(`.brand-toggle`)。畳まれた
  メニューを開く手がかりが右端のハンバーガーしか無いと、帯のどこを押せばよいのか探すことになる。
  重ねるのは `<label for="navbar-toggler">` なので、**開閉は今までどおり CSS だけで成立する**
  (JS も対話回線も要らない)。**ロゴの `<a>` を `<label>` で包まない** ——
  label に別の対話要素を入れるのは HTML として不正なので、**同じ位置に重ねて上から受け止める**。
  敷くのは**ロゴの上だけ**で、帯の左にある戻る/進む(PWA)とハンバーガーは覆わない。
  **PC 幅ではラベルを出さない**(ロゴはホームへのリンクのまま)——
  スマホでロゴがホームへ行かなくなるぶんは、メニューの中の「ホーム」が受け持つ
- **開いているあいだハンバーガーの印は ✕ にする**(3本線のままだと、押した結果が
  帯の見た目に出ない)
- **リンクでページへ移ったら閉じる**(`nav-menu.js`)。開いたままだと遷移先の本文が
  メニューに隠れ、読むたびに閉じる操作が要る。**閉じるのはリンクを押したときだけ** ——
  グループの開閉(`summary`)や外側の押下では閉じない(子を探している最中に畳まれると困る)。
  記憶(`sessionStorage`)も同時に閉じた状態へ倒す —— 倒さないと遷移先で復元されて開き直る。
  **`document` に1回だけ登録する**(メニューの DOM は enhanced navigation で差し替わるため)
- **メニューの外を押したら閉じる**(`.nav-backdrop`)。開いているあいだだけ画面いっぱいに
  敷く `<label for>` で、これも CSS だけで成立する。**面を敷かずに「外側の押下」は拾えない**
  —— 敷かないと本文のリンクのほうが反応する。**PC 幅では出さない** ——
  常に開いていて「外側」が無く、しかも開閉の状態は `nav-menu.js` が幅に関係なく復元するので、
  スマホで開いたまま PC 幅で開くと面だけが残る。
  面は `position: fixed` なので**ロゴの帯・メニュー・ハンバーガーは `z-index` で手前に出す**
  (静的な要素は positioned な面の下に潜る)
- **スマホではロゴの帯(とメニュー)を上端に貼り付ける**(`.sidebar` を `sticky`)——
  一覧は数百行あり、下まで読んだあとメニューへ戻るのに一番上までスクロールし直すことになる。
  **`fixed` ではなく `sticky`** にするのは、帯のぶんの高さが流れに残って本文が下から
  始まるため(`fixed` だと本文の先頭が帯の下に潜り、余白で押し下げる調整が要る)。
  貼り付けたぶん**メニュー自身は画面の高さで頭打ちにして中でスクロールさせる**
  (`max-height: calc(100dvh - 4rem)`)—— はみ出したぶんは貼り付いていて届かない

**収集のボタンは設定にすべて集めてある**(トレンド・興味トピック・定番のどれも)。
一覧を読む画面にジョブのボタンを置くと、**読みに来た人が押す場所と、集めなおしたい人が
探す場所が混ざる** —— 代わりに各一覧には `Components/CollectionHint.razor` で
設定への導線を出す(`Job` にどのボタンで集めなおすかを渡す。文言は `JobRunner.Name` に合わせる)。
**設定の中の並びはサイドバーと同じ順**(ホーム → トレンド → 興味トピック → 定番 →
トピック)で見出しを立てる —— 10 個を平らに並べると「どの画面のものを集めるボタンか」が
読めない。
かつて興味トピック側(論文・イベント・書籍)だけ各ページに置いていたのは、トピックの
選択と地続きで示せるほうが分かると考えたため。**その説明は導線の文と設定側の注記に移した**
(「選んでいないと 1 件も集まらない」)。
**語彙の整備(「話題度を取り直す」「タグを仕分けなおす」)も設定に置く** ——
`/settings/topics`・`/settings/tags` は設定の下のページだが、**ジョブのボタンは1か所**という規則を
そこだけ崩す理由が無い。両ページには行き先を書いた案内文だけを残す。
**説明(手順・算出方法・副作用)はボタンと一緒に動かす** —— 押す前に読むものなので、
ボタンから離すと読まれない。

ジョブ1件の行は共通コンポーネント `Components/JobButton.razor`
(**[定期実行のチェック + 実行順の番号] [実行ボタン] [進捗・結果]** を横に並べる)。
**静的 SSR のフォーム POST**で、対話回線(WebSocket)は張らない —— このアプリは
`<Routes />` にレンダーモードを指定しておらず**全ページ静的 SSR** なので、
ボタンのためだけに回線を張るのは釣り合わない。

- **ページのジョブは1つのフォームにまとめ、行の部品は form を作らない。**
  チェックとボタンを横に並べる以上、部品ごとに form を持つと入れ子になる(HTML として不正)。
  押されたのがどのボタンかは **submit の value** で見分ける(`run:<キー>` / `save-schedule`。
  タグの画面と同じ流儀)—— `FormName` を行ごとに分ける作りはやめた
- **「実行」を押したときチェックは保存しない**(押した操作だけをする)。
  保存は「定期実行の設定を保存」で明示的に行う
- **チェックの横に実行順の番号を出す** —— 画面の並びは軸ごと(サイドバーと同じ)で、
  実行順(データの依存順)とは違うため。番号の出どころは `ScheduledJobs.InOrder` の位置
- **`ScheduleKey` を渡さない行にはチェックを出さない**(「定期実行を今すぐ実行」は
  自分自身を定期実行に入れられない)
- **実行中の印と結果の文言は「入口」ごとに持つ**(`JobRunner` の `IsRunningOperation` /
  `LastMessageOf`。入口の名前は `JobButton` の `Operation`)—— **1つの Runner に入口が
  2つある**ジョブ(トピックの整備 = 話題度 / 仕分け)で、片方を走らせたときにもう片方の
  行にも「実行中…」と結果が出ていた。**直列化(`IsRunning`)は Runner 全体のまま** ——
  同じ DB を触るので入口が違っても同時には走らせない。押せない側の行には
  「別の操作が実行中です。」と理由を書く(黙って disabled だと壊れて見える)

- **ジョブはバックグラウンドで走らせる**(`JobRunner.StartInBackground`)。応答を返し切るまで
  画面が出ない静的 SSR で数分のジョブを await すると、押した人は白い画面を待たされる。
  ページのハンドラは開始だけして戻る
- **実行中は `meta refresh`(3秒)で自動リロード**し、進捗(`JobRunner.Progress`。
  「未知の語 120 件を LLM で分類中: バッチ 1/2…」)をボタンの隣に出す。JS は使わない。
  リロード先の URL を明示した GET にするのは、POST 応答の再読み込みでフォームが
  再送されるのを避けるため
- **結果の文言は Runner が持つ**(`LastMessage`/`LastError`)。ページのフィールドに
  置くと、自動リロード(GET)のたびに消えてしまう

## 外部データソース

| 用途 | ソース | 備考 |
|---|---|---|
| イベント | connpass API / Doorkeeper API / TECH PLAY の RSS | connpass は要申請、他は不要。**主催者は3つとも取れる**(TECH PLAY は `dc:creator`)が参加者数は前2つだけ。**引き方は検索・グループ購読・面掃きの3つ** |
| 書籍 | openBD / Google Books API / 楽天ブックス / Qiita API | 楽天はレビュー、Qiita は推薦本 |
| 記事 | 各種 RSS / Atom | Qiita・Zenn・はてブ テクノロジー等 |
| ニュース | 各種 RSS | Publickey・ITmedia NEWS・InfoQ Japan・CodeZine |
| 論文(トピック検索) | arXiv API / J-STAGE API | どちらもキー不要。間隔は 3 秒以上空ける |
| 話題の論文 | Hugging Face Daily Papers | キー不要。**トピックの選択に依存しない**(1 回 1 リクエスト) |
| 出版トレンド | 国立国会図書館サーチ(NDL サーチ) | キーも申請も不要。**検索語も要らない**(NDC と刊行日で引く) |

外部 API を叩くコードを書くときは、**User-Agent に個人のメールアドレスを入れないこと**。
連絡先が必要な場合はリポジトリ URL のみを記載する。

収集まわりの共通の守り(理由はコードのコメントにもある):

- **何も集まらなかった理由は結果に載せる**(`CollectionRunResult.Note`)。
  **例外にしない** —— 選んだトピックが無くて集まらないのは設定どおりの動作であって
  失敗ではないし、「失敗:」と出ると同じ状況の他のジョブと文言が食い違う
  (以前は論文だけ例外を投げていて、イベント・書籍とメッセージが揃っていなかった)。
  トピックを検索語にする3つ(論文・イベント・書籍)は `CollectionRunResult.NoTopics` を返す

- **取り込む URL は `WebUrl`(Core)で http/https だけに絞る**。`Uri.TryCreate` は
  `javascript:` も絶対 URI として通し、画面の `href`/`img src` にそのまま出るため。
  新しい収集元を足すときもパーサで `WebUrl` を通すこと
- **HttpClient には `MaxResponseContentBufferSize` を掛ける**(`Program.cs` の
  `MaxResponseBytes`)。上限なしだと、収集先が侵害されて巨大応答を返したとき
  swap の無いホストで OOM になる
- **`System.Net.Http.HttpClient` カテゴリのログは Warning に落としてある**
  (appsettings.json)。既定の Information はリクエスト URI を出すので、
  クエリ文字列でキーを渡す API(Google Books・楽天)のキーがログに残るため

連携先とキーの状態は `/settings/integrations`(設定 → 外部連携)にまとめて出す。一覧は
`IntegrationCatalog` に**手で並べてある** —— 「未設定だと何が起きるか」は設定値には書いて
いないし、キーの要否は収集元ごとの事情(申請の要不要・無料枠)で決まるため自動生成できない。
**外部 API を足したらここにも1行足すこと**(キーがあるなら `EditableSecrets` にも)。
画面に出すのは**キーの有無だけ**で、値は長さも先頭数文字も出さない。
**例外は秘匿値でないキー**(`EditableSecret.Sensitive: false` —— ntfy のベース URL・
トピック名)で、保存済みの値を入力欄に出し、隣の「コピー」で写せる ——
設定ミスに気づけないほうが害が大きく、トピック名は購読する端末へ渡すために要るため。
コピーは JS の上乗せ(`wwwroot/copy-field.js`。ボタンは `hidden` で描き、JS が外す)で、
**動かなくても値は見えているので手で選んでコピーできる**。
**自分で決めてよい値は「生成」で作れる**(`EditableSecret.Generate`。今は ntfy の
トピック名だけ)。**作るのは入力欄までで保存はしない** —— 押し直して選び直せるし、
設定済みのトピックを 1 クリックで置き換えてしまわないため。

**キーの設定の入口は同じ画面の「キーの設定」列だけ**(`ApiCredentials`)。値は Data
Protection で暗号化して DB の `Secrets` へ保存し(平文は置かない。詳細は
[docs/database.md](docs/database.md))、**再起動なしで反映される** —— 各収集元・LLM は
キーを起動時に固定せず、実行のたびに `ApiCredentials` から解決するため。
**環境変数・.env・user-secrets ではキーを渡せない**(かつてはフォールバックとして
読んでいたが、入口が 2 つあると「どちらの値が効いているのか」の説明を画面が持ち続ける
ことになるためやめた。Options クラスからもキーのプロパティは消してある)。
接続文字列・モデル名・収集元ごとの待ち時間など、**キーと定期実行(時刻・ジョブの
チェック)・公式イベントの主催者名簿・購読するグループの名簿・面掃きのオン/オフ以外の
設定は今までどおり環境変数**。
キー未設定の収集元は**その収集元だけスキップ**され、ジョブのボタンは消えずに残る。
ntfy の接続先(ベース URL・トピック名・トークン)も同じ画面から設定する
(`Ntfy__ClickUrl` だけは「この環境の公開 URL」というデプロイ側の事実なので環境変数)。

画面は**軸で2節に分ける**(トレンドで使う / 興味トピックで使う)。節の中は用途
(記事・書籍など)でまとめる。どの軸で使うかは `Integration.Axis`
(`IntegrationAxis` は `[Flags]`)で持ち、**複数の軸で使うものは全部の節に出す** ——
「トレンドが動かない」「興味トピックが動かない」「定番が動かない」で見に来る場所が
違うので、片方だけに置くと他から辿れない。複数の節に出るのは話題度の材料
(Qiita トレンド・はてブ人気エントリー)と LLM(トレンド × 興味トピック)、
書誌の補完(openBD・楽天 —— 興味トピック × 定番)。Qiita 推薦本は定番だけ。

収集対象のフィードは `src/TechAntenna.Web/appsettings.json` の `Collection` セクションで
設定する(巡回の時刻は設定画面の「定期実行」)。

connpass は API v2(`X-API-Key` と `User-Agent` が必須)。キーは外部連携の画面から設定する。
キー未設定なら connpass からは収集しない(他の収集元は動く)。

Doorkeeper は `Authorization: Bearer` にアクセストークンが必要(外部連携の画面から設定)。
レート制限は
認証済みで 300 リクエスト / 300 秒。**API は alpha 扱いで破壊的変更が予期されている**ため、
レスポンス形式が変わりうる前提で読むこと。

### Doorkeeper API Terms of Use で課される義務

規約(<https://www.doorkeeper.jp/developer/api> の API Terms of Use)のうち、**コードを見ても
分からないもの**を挙げる。`/terms`・`/privacy`・`/personal_information` は Doorkeeper に
投稿する側と Doorkeeper 社自身の個人情報取扱いの規定で、API 利用者への条件はこちらにある。

- **イベントを表示するときは必ず Doorkeeper のイベントページ(`public_url`)へのリンクを
  含め、そのリンクに `nofollow` 等を付けてはならない**(Must Have Information)。
  外部リンクの扱いを変えるときは規約違反にならないか確認する
- **説明文は取り込まない**。API 経由のコンテンツは Doorkeeper とその顧客に帰属する
  (Ownership)ため、取り込むのはイベントの事実情報(タイトル・URL・日時・会場)だけに
  とどめる。書籍で書誌事実だけを取り込むのと同じ方針
- **外部公開するなら、このアプリ自身の利用規約とプライバシーポリシーが必要**
  (Responsibility)。個人が手元で動かす分には不要だが、公開の前提条件として残しておく
- **Doorkeeper と競合するサービスに使ってはならない**(Restrictions)。本アプリは
  イベント管理・集客ではなく記事/イベント/書籍の横断発見なので競合しないと解しているが、
  イベント検索側を作り込むときは読み直す

**両方ともキーワードごとに問い合わせ、見つかったイベントにその検索キーワードをタグとして
付ける**。同じイベントが複数のキーワードで見つかったら URL でまとめてタグを足す。
connpass は `keyword_or` で全キーワードを1リクエストにまとめることもできるが、それだと
どのキーワードで見つかったか分からず、`hash_tag` が無いイベントがタグ無しになって
トピック横断に乗らないため、リクエスト数と引き換えにキーワードごとに引いている。

**Doorkeeper は検索語がタイトルに実際に含まれるものだけを取り込む**(`KeywordMatcher`)。
`q` が説明文まで検索し、しかも記号を落としてから照合するため、そのまま信じると
タグが意味を失うから —— 実測では `q=C#` が `q=C` と同じ結果を返し(`#` が無視される)、
`q=.NET` は説明文中の `https://…​.net/` という URL に当たっていた。C#/.NET/Blazor で
38 件取れたイベントは、タイトルに検索語を含むものが 0 件だった。

`KeywordMatcher` は単純な部分一致ではなく、**検索語の端が英数字のときだけ、その側が
英数字と地続きでないことを求める**。「AI」が `Rails`・`email` に誤爆するのを防ぎつつ、
語が空白で区切られない日本語(「生成AI最新ニュース」)と記号を含む語(`C#`・`.NET`)を
同じ規則で扱うため。

収集キーワードは AI 系(`AI` / `生成AI` / `LLM` / `AIエージェント` / `機械学習`)。
**C#/.NET は記号がトークナイズで落ちる収集元があり検索語として使いにくい**。
アプリの実装言語と、アプリが集める情報の対象は別。

### TECH PLAY(RSS)

**キーも申請も要らない代わりに検索ができない**。最新のイベントが 50 件流れてくるだけなので、
巡回して差分を溜めることで広く拾う(過去には遡れない)。企業主催のウェビナーはこの経路が
一番厚く、connpass / Doorkeeper に載らないベンダー系のイベントが入る。

- RSS 2.0 だが**開催日時・会場・住所は独自名前空間 `tp:`**(`https://rss.techplay.jp/`)に
  あり、標準の要素からは取れない。記事用の `FeedParser` と別実装(`TechPlayFeedParser`)なのは
  このため。`tp:` の日時には時差の表記が無く**日本時間**なので、読むときに UTC へ直す
- **タグは `<category>` から作る**(他の収集元は検索キーワードをタグにする)。ただし
  `IT`・`テクノロジー`・`イベント` は全イベントに必ず付いていて横断に使えないため落とす
- **主催者は `dc:creator` から取れる**(実測: 50 件すべてに入っており、企業名がそのまま来る)。
  RSS 標準の要素でも `tp:` の独自要素でもない**Dublin Core** なので長らく見落としていて、
  「TECH PLAY は主催者が取れない」と書いていた —— **参加者数を持たないこの収集元にとって、
  公式かどうかが唯一の重み**なので、取れないあいだはベンダーのウェビナーが注目度で沈んでいた
- **参加者数は今も取れない**(RSS に無い)。null のまま置く(0 と混ぜない)
- 設定は `TechPlay` セクション(`FeedUrl`)。空なら収集しない

規約([利用規約](https://techplay.jp/terms_of_use))は Doorkeeper と違い、**取得・再利用を
明示的に許諾していない**。第7条3項が「当社の許諾がない限り、当社著作物等の全部または一部の
利用、複製、転載等を行うことができない」とする一方、クローリングや RSS 利用を禁じる条項は
無い。取り込むのはイベントの事実情報(タイトル・URL・日時・会場・主催者・参加者数)だけに
とどめ、**公開するなら事前に問い合わせること**。

### イベントの3つの引き方(検索 / 購読 / 面掃き)

**キーワード検索だけでは、注目度の高いイベントが構造的に落ちる。** 収集語は AI 系
(`AI` / `生成AI` / `LLM` …)だが、年1回の大型カンファレンスの名前は固有名詞
(`RubyKaigi`・`DroidKaigi`・`JJUG CCC`)で、収集語をタイトルに持たない。
結果、名前に「AI」が入る小さな勉強会ばかりが残る。そこで引き方を3つ持っている。

| 引き方 | 何を手がかりにするか | 実装 | トピックの選択 |
|---|---|---|---|
| 検索 | 選んだトピックを検索語にする | `ConnpassEventSource` / `DoorkeeperEventSource` / TECH PLAY | **要る**(空なら集まらない) |
| 購読 | **グループの名簿**(`FollowedGroups`) | 上の2つが名簿ぶん追加で引く | 要らない |
| 面掃き | **期間で全件 + 参加者数のしきい値** | `ConnpassSweepEventSource` / `DoorkeeperSweepEventSource` | 要らない |

- **購読と面掃きで入ったイベントは `TechEvent.PickedBy` に理由を持つ**(購読しているグループの
  表示名、「参加者 100 人以上」)。**判定の印ではなく理由の控え**で、カードにそのまま出して
  「なぜトピックに当たらないイベントがここにいるのか」を説明させる ——
  説明が無いと絞り込みが壊れているようにしか見えない
- **`PickedBy` が付いたものはトピックの絞りを通さない。** 検索語で見つけたのではないので
  選んだトピックのタグを持っておらず、絞ると経路ごと無効になる(実装しても1件も増えない、
  という壊れ方をする)。**絞りは収集(`EventCollectionRunner`)と表示(`Events.razor`)の
  2か所にあるので、両方で同じ規則にすること**
- **購読・面掃きで入ったイベントのタグはハッシュタグだけ**になる。グループの表示名を
  タグにはしない —— イベント名が語彙に流れ込むと、タグの一覧と LLM の仕分けが
  その回限りの固有名詞で埋まる
- **`IEventSource.WorksWithoutTopics`** が「トピックの選択が空でも集めるものがあるか」。
  ランナーはこれを見て、選択が空のとき検索しかできない収集元を**叩きに行かない**
  (集まらないと分かっている相手にリクエストを投げない)
- **定番のイベント(`/classics/events`)を埋めるのは購読と面掃き。** 検索で入るものは
  選んだトピックのイベントなので、この2つが空だと定番の一覧が興味トピックと同じ顔ぶれになる

#### 購読の名簿(`FollowedGroups` / 設定 → 購読)

書式は `<収集元>:<識別子> <表示名>` の1行1件で、置き場は `Secrets`
(`Events:FollowedGroups`)。公式の名簿(`OfficialOrganizers`)と同じ流儀だが、
**初期値を持たない** —— シリーズ ID もコミュニティ名も実在の値なので、リポジトリに
憶測で書けない(未設定なら「この経路では何も集めない」が正しい状態)。

- connpass は**サブドメインでも数字のシリーズ ID でも書ける**。サブドメインのときは
  `/api/v2/groups/?subdomain=…` で1回だけ引き直し、**プロセスの間ずっと覚えておく**
  (グループの ID は変わらない)。サブドメインを許すのは、**数字の ID が connpass の
  画面に出てこない**ため —— 人が名簿を作れなくなる
- Doorkeeper は `GET /groups/<名前>/events` を直接引く。**タイトルの照合(`KeywordMatcher`)は
  しない** —— 検索語で引いていないので照合する相手が無いし、「このコミュニティのイベントは
  全部見たい」が購読の意味そのもの
- **読めない行・存在しない識別子は黙って飛ばす。** 1行の打ち間違いで保存や収集全体を
  止めない代わりに、読めなかった行は設定画面に出して直せるようにしてある
- **公式の主催者から候補を出せる**(`FollowSuggestions`。画面の「公式の主催者から足す」)。
  **「公式」は集めたイベントに付く印でしかなく、公式のイベントを狙って取りに行く経路は無い** ——
  提供元のイベントが載るかどうかが、たまたま検索語に当たったかどうかに委ねられていた。
  グループを購読すればトピックに関係なく全部入るので、**すでに集まっているイベントのうち
  公式と判定された主催者**を候補にして名簿へ渡す。
  **識別子は保存済みイベントの URL から起こす**(connpass も Doorkeeper もサブドメインが
  グループ)—— 主催者名から ID は引けないし、引けば外部への問い合わせが増える。
  **収集元は URL のホストで見分ける**(`SourceName` は経路ごとに名前が違うので、
  そちらで分岐すると経路を足すたびに直すことになる)。
  グループを持たない URL(`https://connpass.com/event/…`・TECH PLAY)は候補にしない。
  足すのは**末尾への追記だけ**で、書いてある行(メモを含む)は消さない
- **購読を外しても、すでに集めたイベントは一覧から消えない**(`PickedBy` は保存された
  「集めた理由」なので、名簿を引いても消えない)。トピックの選択を外したときは表示からも
  外れるので、そこだけ扱いが違う —— **公式の名簿(`OfficialOrganizers`)とも逆**で、
  あちらは判定を保存せず表示のたびに突き合わせる。判定(公式かどうか)は後から変わりうるが、
  「わざわざ追いかけると決めて集めた」という事実は後から変わらないため

#### 面掃き(`ConnpassSweepEventSource` / `DoorkeeperSweepEventSource`)

期間で全件取り、**参加者数のしきい値で切る**。検索語も名簿も要らないので、
**名前を知らない大型イベントを拾える唯一の経路**。**設定は両方とも同じ形**
(`EventSweepOptions` を `Connpass:Sweep` と `Doorkeeper:Sweep` で共有する)——
相手が違うだけで決めることは同じだから。

| | connpass | Doorkeeper |
|---|---|---|
| 引き方 | `ym=YYYYMM` で月ごと | `since`/`until` の期間で `q` を付けずに引く |
| 終わりの分かり方 | 返った件数と `results_available` | **空のページ**(総件数を返さない API のため) |
| タグ | ハッシュタグだけ | **付かない**(ハッシュタグの概念が無い) |

- **既定ではどちらも動かさない。オン/オフは画面(設定 → 外部連携の「状態」列)** ——
  `SweepSettings` が DB(`Secrets` の `Events:Sweep:Connpass` / `Events:Sweep:Doorkeeper`)に持ち、
  **環境変数では設定できない**(キー・定期実行・名簿と同じ扱い)。1 回で数十リクエストかかり、
  相手のレート制限(どちらも実質 1 秒 1 リクエスト)と相談することになるため既定は無効。
  **appsettings の `Connpass:Sweep` / `Doorkeeper:Sweep` に残っているのは数の設定だけ**
  (しきい値・何か月ぶん・待ち時間)
- **収集元は無条件で登録し、走らせるかどうかは実行のたびに読む**(キーと同じ)——
  起動時に見て分岐すると、画面で入れても再起動するまで効かない。
  **止めているあいだは `WorksWithoutTopics` も false** にする。true のままだと、
  面掃きを止めていてもランナーが「トピックが空でも集めるものがある」と判断し、
  「トピックを選んでいないので集まりません」の案内が出なくなる
- **参加者数が `null`(取れていない)ものは残さない。** この経路は「人が集まっている」ことだけを
  根拠に拾っているので、根拠が無いものを通すと単なる全件取り込みになる
- ページ数には上限がある。**打ち切ったら `Truncated` に残す** ——
  黙って切ると「全部見た」と読めてしまう
- **`q` を省けるのが Doorkeeper 側の要点。** 検索でしか引けないと思い込んでいたが、
  `/events` は期間だけでも引ける —— **収集元を足さずに経路を1本増やせた**

### イベントの重みづけ(公式・参加者数・記事の言及数)

イベントには**記事のはてブ数のように単独で順位になる数値が無い**ので、
**「提供元が出す一次情報か」「どれだけ人が集まっているか」「外でどれだけ書かれているか」の3つ**を
合わせて測る(`EventPopularity`)。

| 材料 | connpass | Doorkeeper | TECH PLAY |
|---|---|---|---|
| 主催者(`TechEvent.Organizer`) | `group.title`(無ければ `owner_display_name`) | `expand[]=group` の `group.name` | **`dc:creator`**(RSS 標準でも `tp:` でもなく Dublin Core) |
| 参加者数(`TechEvent.ParticipantCount`) | `accepted` | `participants` | **取れない** |

- **「公式」は判定結果ではなく主催者名を保存し、表示のたびに名簿と突き合わせる**
  (`OfficialOrganizers`)—— 名簿は画面(設定 → 主催者)から直せるので、直した結果が
  過去のイベントにも効いてほしい(`RawTags` から `Tags` を作り直すのと同じ考え方)。
  名簿の置き場は `Secrets`(`Events:OfficialOrganizers`)で、**環境変数では設定できない**。
  未設定なら `OfficialOrganizers.Defaults`(リポジトリに持つ初期値)
- **突き合わせは `KeywordMatcher` の部分一致。**「Microsoft」で「日本マイクロソフト株式会社」を
  拾いたいので完全一致にはできない。**誤って公式になる主催者は出る**ので、
  設定 → 主催者に**実際に集まっている主催者を件数つきで出して**(`GetOrganizerCountsAsync`)、
  判定を見ながら名簿を直せるようにしてある
- **補欠(`waiting` / `waitlisted`)は参加者数に足さない** —— 足すと「定員 10 人・補欠 200 人」が
  大規模イベントとして扱われる
- **`null` は「取得していない」、`0` は「まだ誰も参加していない」で別物**(はてブ数・レビュー数と同じ)。
  ただし**注目度の計算では null を 0 人として扱う** —— 書籍のように null を後ろへ回すと、
  数を持たない TECH PLAY のイベントが公式判定ごと沈むため。そのぶん
  「注目度順では数の取れない収集元が後ろに来る」ことを画面に書いてある
- **注目度 = 公式の下駄(参加者およそ 9 人ぶん)+ 参加者数の対数 + 記事の言及数の対数 × 2.5。**
  対数にするのは 500 人のカンファレンスを 50 人の勉強会の 10 倍に扱わないため。
  **同点なら開催日の早い順**

#### 記事の言及数(`EventMentions` / `EventMentionRefresher`)

**参加者数だけでは、注目度の高いイベントほど沈む。** 数を出すのは connpass と Doorkeeper だけで、
自社サイトで完結する大型カンファレンスはそもそも参加者数を公開しない。
一方で**このアプリは記事をすでに集めている**ので、「そのイベントについて何本書かれたか」は
収集元を問わず測れる —— **記事とイベントを同じ場所に集めているからこそ持てる指標**で、
イベント専門のサービスには真似しにくい。

- **数え直しは外部を一切叩かない。** イベント収集の最後に、手元の記事と突き合わせるだけ。
  定期実行の並びでは記事の収集がイベントより先なので、その日集めた記事まで数に入る
- 照合語は**タイトルから作る**(`EventMentions.KeyFor`)。飾り(`【…】`)・回数(`第N回`)・
  年・ハッシュタグを落とし、副題の区切りより前を名前とみなす。
  **ハッシュタグは使わない** —— `RawTags` の中では検索キーワードと区別が付かず、
  「タグの2番目」のような当て推量に頼ることになる。**規則は1本にする**
- **照合語を作れなければ測らない**(`MentionCount` は null のまま)。短すぎる語(「AI」)、
  一般名(「もくもく会」)、技術名そのもの(語彙にある「Kubernetes」)は捨てる ——
  **誤った言及数は注目度の並びを静かに壊す**。技術名を捨てるのに `TopicCatalog` を見ている
- **`null` は「測っていない」、`0` は「まだ書かれていない」で別物**(参加者数と同じ規則)。
  ただし注目度の計算では null を 0 本として扱う(後ろへ回すと、名前が短いだけのイベントが沈む)
- **重み 2.5 の意味**: 参加者数への換算でおおよそ**言及 1 本 = 8 人 / 3 本 = 32 人 /
  10 本 = 400 人**。「10 本書かれた年1回のカンファレンス」が「400 人集まった勉強会」と釣り合う。
  **理屈で決められる値ではない**ので、動かしたら実際の一覧を見比べること
- **既存のイベントは収集のたびに主催者と参加者数だけ取り込む**(`IEventStore.AddRangeAsync`)。
  参加者数は開催が近づくほど増えるので、初回の数のまま置くと注目度が古くなる。
  書誌にあたる情報(タイトル・日時・会場)は上書きせず、**取れなかった回に null で
  上書きもしない**(書籍の `BookMerge` と同じ方針)

画面(`/interests/events`)は**上がカレンダー、下が一覧**。イベント1件のカードは共通の
`Components/EventItem.razor`(トピック・タグの詳細ページでも使う)で、**公式はバッジ、
参加者数は3段階のバッジ**(30 人 / 100 人。記事のはてブ数と同じ文法)、
**言及数は破線のバッジ**(参加者数と同じ見た目にすると、「200 人」と「記事 3 本」が
同じ尺度の数に見える)、**`PickedBy` は塗らない注記**(目立たせる対象ではなく、
疑問が湧いたときに読めればよいもの)。
一覧の**並びは開催日順が既定で、タブで注目度順**にできる —— 注目度順を既定にすると
来月の大きなイベントが先頭に来て、明日の予定を見落とす。

### 定番のイベント(`/classics/events`)

**同じデータを、トピックの選択で絞らずに注目度で切って出す**もう1枚の一覧
(`Components/Pages/ClassicEvents.razor`)。興味トピック側が「自分が選んだ語に当たるもの」
なのに対し、こちらは「選んでいるかどうかに関わらず、外で注目されているもの」。

- **「毎年開かれる定番のカンファレンス」を見分ける方法はまだ無い。** 本来は開催の履歴
  (`RubyKaigi 2025` と `RubyKaigi 2026` を同じシリーズと見る)から拾うべきだが、
  手元に 1 年ぶんの履歴も溜まっていない。**いまは注目度で代用している** ——
  画面にもそう書いてあるので、履歴で拾えるようになったらここごと書き換えること
- **足切りは `EventPopularity.IsNotable`**(公式 / 参加者 30 人以上 / 言及 2 本以上)。
  トピックで絞らない一覧なので、材料を1つも持たないものまで並べると
  「注目度の高いイベント」ではなく**単なる全件**になる。**カードの強調(hot-mid)と
  同じ規則を1か所から出す** —— ずれると「強調されていないイベントが上位に出る」形で表に現れる。
  **落とした件数は画面に出す**(黙って隠すと絞り込みが壊れたように見える)
- **既定は注目度順**(タブで開催日順)—— 興味トピック側と**逆**にしてある。
  あちらは予定を立てる場所、こちらは規模の大きいものから拾う場所
- **カレンダーは置かない。** 日付で見る場所は興味トピック側にある
- **専用の収集ジョブは作らない。** 材料は「イベントの収集」で集めたものそのもので、
  種別の列(`ArticleKind` のような)も増やしていない —— 論文・書籍と違い、
  軸ごとに**探し方が違うのではなく絞り方が違うだけ**だから
- **中身は収集側の絞りに縛られる。** `EventCollectionRunner` は選んだトピックに
  当たらないイベントを `PickedBy` の付いたものしか保存しないので、**購読(`FollowedGroups`)も
  面掃き(`Connpass:Sweep` / `Doorkeeper:Sweep`)も空だと、興味トピックの一覧と
  同じ顔ぶれになる**。そのことは画面(空のときの案内と設定 → 定番の注記)に書いてある。
  購読のほうは**公式の主催者から候補を出せる**ので(`FollowSuggestions`)、
  名簿を人が調べて書き写さなくても増やせる

### イベントカレンダー(月グリッド)

`/interests/events` の上に**その月のカレンダー**を出す。月の移動は `?month=2026-09` の
リンクなので、**全ページ静的 SSR のまま**(JS も対話回線も使わない)。

- 組み立ては `EventCalendar`(Core)。**日付の境界はすべて日本時間**で数える ——
  UTC のまま日付に直すと、日本の朝 9 時までに始まるイベントが前日のマスに入る
- **読み込む範囲は月そのものではなく、前後にはみ出したマスまで**(`GridRange`)。
  月の範囲だけで問い合わせると、月末の週に並ぶ翌月頭のマスが空に見える
- **1マスに積むのは3件まで**(残りは「他 N 件」)—— そのまま積むと、その週の行だけが
  縦に伸びてカレンダーの形が崩れる。マスの中のタイトルは1行に省略し、全文は
  マウスオーバーと下の一覧で読ませる
- **月を指定したときは一覧もその月になる**(過去の月も出る)。指定していないときは
  従来どおり「これからのイベント」—— カレンダーで前の月を開いたのに一覧が未来のままだと、
  上下で別のものを見ていることになる
- **狭い画面でも列は7のまま**、マスの中は色帯だけにする(縦に積み替えると「週」の形が消えて
  カレンダーとして読めなくなる)。公式のイベントは帯の色で示す(バッジの字は入らないため)

### 記事の種別(`ArticleKind`)

記事・ニュース・論文は**同じ `Article` として保存し、`Kind` で分ける**(画面と、
要約の対象かどうかにだけ効く)。フィードの種別は `Collection:Feeds` の `Kind` で指定する。

**論文は2種類あり、収集の起こし方も画面も分けてある。**

- **話題の論文**(`ArticleKind.TrendingPaper`。Hugging Face Daily Papers)——
  トピックの選択に依存しないので、収集は**トレンドの収集に含める**(`IArticleSource`)。
  画面はトレンドの下(`/recent/papers`)。**日本語の論文は入らない** ——
  中身は arXiv への投稿で事実上すべて英語(実測: 50 件中 0 件が日本語)
- **トピック検索の論文**(`ArticleKind.Paper`。arXiv / J-STAGE)——
  検索なので収集対象に選んだトピックが要る。**専用のボタン**(`IPaperSource` /
  `PaperCollectionRunner`)で、画面は興味トピックの下(`/interests/papers`)。
  選択が空なら理由を出して止まる —— 以前は記事と同じボタンで、
  「記事は集まったのに論文だけ 0 件」の理由が画面から分からなかった

画面は**親子の2段**で、**並びで役割を分ける**。`/recent/general`「トレンド / まとめ」が一望のハブで、
**興味のあるトピックの外も含めて**直近 7 日の話題をニュース→記事→論文の順に、
各セクション**話題度の高い順**(ニュース・記事ははてブ数、論文は upvote。大きいほうで
測るのはカードの強調と同じ規則)で**5 件ずつ**出す —— ここは「何が話題か」に答えるページ。
全量は子ページ(`/recent/news`・`/recent/articles`・`/recent/papers`、3ルートで
1 コンポーネント `ArticleListPage`。出版のテーマは `/recent/books`)が受け持ち、**新着順(既定)と話題度順をタブで
切り替えられる**(`?sort=popular`)—— 「読み逃したものを追う」と「反応の大きかったものを拾う」は
どちらも要る。**並びを2つ持つなら、いまどちらを見ているかを画面に出すこと**
(かつて新着順だけにしていたのは、それが読めなくなるのを避けるためだった。タブがその答え)。
話題度順でも**窓は直近 100 件**(`Limit`)に切る —— 全期間から拾うと昔の当たり記事が
居座って動かなくなる(トレンドが 7 日で切っているのと同じ理由)。
**`/interests/papers` にはタブを出さない** —— arXiv / J-STAGE の論文には数値が付かず、
並べ替えても新着順とほぼ同じで選択肢が嘘になる。カードは共通の
`Components/ArticleCard.razor` で、**興味のあるトピックに当たる記事はタイトル前の黄色い★**
(カード全体を染める強調は話題度専用にした —— 2つの意味で同じ強調を使うと読めない)。
収集・要約・翻訳のボタンは設定に集めてある。

- **論文の要旨(abstract)は取り込む。** **arXiv のメタデータは CC0** ——
  API Terms of Use が「descriptive metadata について CC0 1.0 の下で自由に利用できる」と
  明記している(HF Daily Papers の `summary` も同じ要旨)。書籍の `description` を
  取り込まないのは出版社の著作物だからで、事情が違う。**J-STAGE の要旨は取り込まない**
  —— 権利が掲載誌ごとで、API 規約に再利用の明示がない
- **要旨があるので論文も要約できる**(`GetUnsummarizedAsync` は「要旨がある行」を返す)。
  かつて論文を一律に除外していたのは材料が無かったからで、理由が消えた
- **論文の upvote 数(`Article.UpvoteCount`)は、はてブ数とは別の列**にした
  —— 母集団も測っているものも違い(日本語圏の記事の反応 / 論文コミュニティの反応)、
  同じ列に混ぜると 2 つの意味が 1 つの数字に潰れる。
  トレンドは常に話題度の高い順、`/recent/papers` は新着順が既定でタブから upvote 順にできる。
  カードの強調は**両者の大きいほう**で測る(片方だけ見ると論文が常に地味になる)。
  並べ替えも同じ「大きいほう」で測るので、種別ごとに列を選び分けなくてよい
- **arXiv には英語の検索語を投げる**(`TopicCatalog.EnglishTermOf`)。日本語の正式表記を
  そのまま投げると 0 件になる(実測: `生成AI` で 0 件)。J-STAGE は和文の索引なのでそのまま
- **J-STAGE の Atom は `FeedParser` では読めない。** entry の中身が独自要素
  (`article_title`/`article_link`)で標準の `title`・`link` を持たないため、専用のパーサにしている

### 本になっているテーマ(出版トレンド)

**最近出た本・ムックのタイトルから数えたテーマ**を出す。記事の話題度が「速い反応」なら、
こちらは**企画から刊行まで数か月かかる、遅くて重い動き** —— 同じトレンドでも時間軸が違うので、
記事のセクションとは見た目も変えてある(カードではなく行)。

**置き場は2つで、まとめ ⇔ 子ページの分担は他の種別と同じ。**
`/recent/general`(トレンド / まとめ)の最後には**上位 12 テーマと代表タイトル 2 件ずつ**を
1 テーマ 1 行で置き、「すべて見る →」で `/recent/books`(トレンド / 書籍。`TrendBooks.razor`)へ渡す。
子ページは**テーマごとの折りたたみ**で、見出しに**そのテーマになっている冊数**、中はその全冊 ——
**見た目は `/interests/books` と同じ**(見出し + 冊数のバッジ + 1 冊 1 行の `BookItem`)にそろえてある。
違うのは**まとまりの単位**だけで、あちらは「収集対象に選んだトピック」、こちらは
**出版側が寄せているテーマ**(選択に依存しない)。行に渡すのは `NewRelease` を `Book` の形に
写したもの —— **保存はしない表示のためだけの変換**で、行の組み立てを 1 か所に保つため
(新刊は書影・著者・レビューを持たないので、出るのはタイトル・出版者・刊行年月・タグだけ)。

- **収集元は NDL サーチ**(`NdlSearchNewReleaseSource`)。**キーも申請も検索語も要らない** ——
  分類(NDC 007 = 情報科学)と刊行日で引けるので、**収集対象の選択に依存しない**
  トレンドの軸に置ける。ジョブは「出版トレンドの収集」(設定 → トレンド)
- **雑誌の号にはテーマが載っていない**(「日経ソフトウエア 2026年9月号」)。テーマが読めるのは
  **ムック・入門書のタイトル**のほう(「今知っておきたい生成AI厳選100ガイド」)なので、
  雑誌かどうかで分けずに集めてタイトルからトピックを拾う ——
  `Periodical` で落としているのは「読むべき本」の一覧のほうで、ここでは材料になる
- **保存先は `Book` ではなく `NewReleases`**(詳細は [docs/database.md](docs/database.md))。
  読ませるためではなく数えるための観測で、混ぜると書籍の一覧が新刊で埋まる
- **窓は直近 6 か月**(`NewReleases:WindowMonths`。収集にも集計にも同じ値を使う)——
  記事の 7 日で切ると 1 冊も出てこない。**同じ窓を毎回引き直して上書きする**ので、
  語彙を変えても次の収集でタグが揃う(再正規化のジョブを持たなくてよい)
- 集計は `PublishingTrend.Themes`(Core)。**1 冊だけのテーマは出さない**(偶然が混じる)。
  同数なら**新しい本を含むほうが先**。`PublishingTheme` は**窓に入った本を全部持ち**
  (`Books`。`Count` はその数)、まとめの節が先頭 2 件を代表として出し、書籍ページが全部を並べる
  —— **数えた集合と並べる集合が同じ**なので、見出しの「N 冊」と中身の行数が食い違わない
- **語彙にある語しか出てこない**(タイトルの突き合わせは `TopicCatalog.FindIn`)。
  それでよい —— ここで見たいのは「知っているテーマのうち、どれが厚くなっているか」で、
  未知語の発掘はタグの仕分けの仕事。実測では 481 冊から AI 131 / 生成AI 40 /
  プログラミング 26 / AIエージェント 21 … と並んだ

### ブックマーク数(`Article.BookmarkCount`)

**人気の指標は、はてなブックマーク数に一本化**している。数値を持つフィードが、はてブの
RSS(`hatena:bookmarkcount`)しか無いため —— Qiita の popular-items も Zenn の /feed も
**人気順の選別はあるが数値は乗らない**(実測)。他ソースの分は、記事収集の最後に
**はてなブックマーク件数取得 API**(キー不要・50 URL/リクエスト)で直近 7 日の
記事・ニュースをまとめて引き直す(`BookmarkCountRefresher`)。論文は対象外(はてブにほぼ載らない)。

- **null は「未取得」、0 は「ブックマークされていない」**で別物(書籍の `ReviewCount` と同じ)
- **種別ページ(ニュース・記事・話題の論文)の既定は新着順**で、タブで話題度順にできる。
  並びに関わらず話題度は目立ち方にも出る —— バッジが件数で3段階
  (10 users 未満 / 10〜99 / 100 以上)、トレンドではカードの面にも同じ段階が出る
  (`ArticleCard` の `EmphasizeByPopularity`)。
  **常に話題度で並ぶのはトレンドだけ**(子ページは選んだときだけ)
- トレンドの窓を 7 日にしているのは、窓が無いと動きの止まった話題が居座り続けるため

### フィードのタグはタイトルから作る

**Zenn の RSS も Qiita の Atom も `category` 要素を持たない**(実測: Qiita 49 件・Zenn 23 件が
すべてタグ無しだった)。ニュースサイトも同様なので、収集元のタグだけに頼るとトピック横断にも
一覧の強調にも乗らない。`TopicCatalog.FindIn` で**タイトルに出てくるトピックをタグに足す**。

- 判定は `KeywordMatcher`(`AI` が `Rails`・`email` に誤爆しない)
- 見るのは**タイトルだけ**。本文まで見ると、文中で一度触れただけの語でタグが付く
- **再正規化も同じ規則で作り直す**(`RawTags` + タイトル)。そうしないと、この規則を入れる前に
  集めた記事がタグ無しのまま残る

## タグによる横断

記事(ニュース・論文を含む)・イベント・書籍は `TagNormalizer` を通した正規化済みタグを持ち、
`TopicService` がそれを突き合わせてトピックの詳細(`/settings/topics/<タグ>`)に出す。
**3種がそろったタグを上位**に出す(件数が多いだけのタグより、記事・イベント・書籍が
全部あるタグを優先する)。

**タグと語彙(トピック)は別のテーブル**にしてある(`Tags` / `Topics`。詳細は
[docs/database.md](docs/database.md))。収集データで見かけたタグはまず `Tags` に入り、
**`Status` を持つ**(仕分けまち / 昇格 / 別名 / 除外 / 保留)。語彙になるのは
昇格したものだけ —— 以前は 1 枚のテーブルに同居していて、状態を列にできず
「行の有無 × カタログに載っているか × 分類記録の種別」から導出していた。

- **観測(件数・話題度)と仕分け(状態)を分けてある。** 収集は状態に触らない ——
  触ると収集のたびに仕分けが巻き戻り、同じ語を何度も LLM に聞くことになる
- **語彙の権威は DB。** `topic-seed.json` は **DB が空のときに流し込む初期値**
  (`TopicSeeder`)で、以後の衝突ルールは持たない。読み取り用のスナップショットが
  `TopicCatalog` で、起動時と整備(話題度の取り直し・タグの仕分け直し)のあとに
  `TopicCatalogRefresher` が DB から組み直す
- **別名は「その語彙へ寄せると決めたタグ」そのもの**(`TopicCatalogBuilder`)。
  別名の一覧を二重に持たず、タグの状態から導出する。おかげで**別名の件数が寄せ先へ
  合算されるのが構造で保証される**(以前は再正規化の副作用で成立していた)

**トピックの画面は設定の下に2枚**(かつては一覧をホームの下に置いていたが、収集対象を
決めるのは設定の仕事なので設定側へ寄せた)。

| ページ | 見せるもの |
|---|---|
| `/settings/topics` | トピック一覧(ツリー/ランキング)・収集対象の選択・**語彙の持ち出し / 取り込み** |
| `/settings/tags` | **見かけたタグ全部**を状態ごとに。**仕分けを手で直せる**・まとめて除外(トピックの子ページ) |
| `/settings/tags/<キー>` | タグ1語の詳細。**仕分けの状態・出どころ・件数**と、紐づいた記事・イベント・書籍 |
| `/settings/topics/<タグ>` | その語の**同義語**と**親子ツリー上の位置**、続けて紐づいた記事・イベント・書籍 |

**タグの話は `/settings/tags` にまとめる** —— 状態ごとの一覧とまとめて除外が同じ画面にあり、
「仕分けまちを見る → ゴミを除外する」までがそこで完結する(押すのは設定。行き先は画面に書く)。

**「未仕分けのタグが N 語あります」の案内は設定(ボタンの上)に出す。** かつては
`/settings/topics` に置いていたが、**そこには押すボタンが無い** —— 気づいても何もできない案内に
なっていた。数はタグの画面と同じ `ITagStore.GetPendingAsync` から取る(画面に出す数と
実際に LLM へ流れる対象を1か所から出すため)。

`/settings/general` は**ジョブのボタンと、子ページ(トピック・主催者・外部連携)への入口**を持つ。
一覧や表は子ページ側 —— 1ページに詰めると、長い一覧と表が混ざって目的の操作に辿り着けない。

### トピックの詳細(`/settings/topics/<タグ>`)

**2つの問いに答えるページ**で、順番に意味がある —— 上が「この語は何で、語彙としてどう
整理されているか」(一言説明・同義語・ツリー上の位置)、下が「この語に何が紐づいているか」
(記事・イベント・書籍)。

- **語彙の姿は `TopicCatalog.StructureOf` が組む**(`TopicStructure`)。ストアではなくカタログを
  見るのは、**語彙の出どころを1つにするため** —— カタログは DB(`Topics` と別名タグ)から
  組んだスナップショットで、起動時と整備のあとに組み直される
- **祖先を辿るのも配下を組むのも訪問済みを見て打ち切る。** LLM の分類は人手で検証されないので、
  親子が循環していても(自分自身を親にしていても)画面が固まらないようにする
- **見出しは正式表記**(`生成ai` ではなく `生成AI`)。カタログに無い語はキーのまま出し、
  「まだ語彙に載っていない平置きの語」だと画面に書く —— 詳細ページ自体は出す
  (タグとしては生きているので、記事の一覧からは辿れる)
- **別名は出どころで見分けられるようにする。** `topic-seed.json` に人が書いた分と、
  LLM が分類した分(`ITopicClassificationStore` の `Alias`)を突き合わせて印を付ける。
  役割が「人が確定させた語彙」と「LLM の自動分類」で分かれているため。
  **色だけで分けない**(字でも `LLM` と出す)
- **配下は直下の子までを開き、孫以下は `details` で畳む。** 「プログラミング」のような大きな語は
  配下が 70 語を超え、開いたまま出すと下の記事・イベント・書籍が画面外へ押し出される。
  畳んだノードには配下の語数を出して、開く前に規模が分かるようにしている

### 収集対象の選択(`IsSelected`)

**収集(検索)に効くのはイベント・書籍・論文。記事は選択で絞らない。** イベント・書籍・
論文は選んだトピックを検索語にして問い合わせる(選択が空なら 1 件も集まらない)が、
記事の RSS は検索ではなく巡回なので、絞っても収集先への負荷は変わらない —— 捨てると
選んだトピックの外が見えなくなるだけなので全部保存し、画面で選択トピックのものに★を付けている。

何を集めるかは `/settings/topics`(設定 → トピック)の**行頭のチェックボックス**で決める(概要は
README「トピック収集」)。**保存の経路は2つあり、既定で効くのは前者**:

- **チェックを押した瞬間に 1 件だけ保存する**(`wwwroot/topic-select.js` →
  `POST /api/topics/select` → `ITopicStore.SetSelectedAsync`)。一覧は数百行あり、
  1 個チェックするたびにページを読み直すのは重い。**1 件だけを更新する**ので、
  閉じた枝や検索で絞られている行の選択を巻き添えにしない
- **フォームの「選択を保存」**(`UpdateSelectionAsync`)は、**一覧に出ている分で丸ごと
  置き換える**(チェックの外れたものは POST に乗らないため)。JS が動かないときの経路として
  残してある —— **保存する手段を画面から消さない**。JS が効いているあいだはボタンを隠し、
  その場保存が失敗したときだけ出し直す(押せる保存が無い状態を作らない)

その場保存まわりの決めごと:

- **パスは `/api/topics/select`。** `/settings/topics/…` の下に置くと
  トピックの詳細(`/settings/topics/{tag}`)と紛れる(`/export/topics.json` と同じ理由)
- **フォーム形式(multipart)で受ける** —— `UseAntiforgery()` が自動で検証するのがこの形だから
  (JSON の本文だと検証されない)。トークンは画面のフォームの隠しフィールドから写して送る
- **語彙に無い語は 404 を返し、画面はチェックを元に戻す** —— 収集対象になっていない語を
  選んだつもりにさせないため。理由は画面の状態表示に出す(失敗の表示は消さずに残す)
- **結果は画面下端に固定して出す**(`.select-status`)。一覧が数百行あるので、ページの
  上や下に置くと、いま押した行のそばに出てこない。成功は数秒で消し、失敗は残す
- **同じ語を続けて押したときは、最後に送った要求の応答だけを反映する**(応答の追い越し対策)

**選択は「収集」と「表示」で効き方が違う。** 親を選んだとき、配下のトピックは
**興味の範囲には入るが、検索語にはならない**:

- **収集(外部への検索)は選んだトピックだけ。** 保存も配下へ広げない
  (`UpdateSelectionAsync` に渡すのはチェックした分そのまま)—— イベント・書籍・論文は
  トピック 1 つごとに外部へ問い合わせるので、配下まで検索すると大きな親を選んだだけで
  リクエスト数が跳ねる。配下も検索したければ、その子を自分でチェックする
- **表示と強調は配下込み**(`TopicCatalog.ExpandWithDescendants`。読む側で毎回広げる)。
  「AI に興味がある」と言っているのに `LLM` や `RAG` の記事に★が付かないのは、選んだ側の
  期待と合わないため。効く場所は 3 つ ——
  **興味トピック配下のページ(`/interests/books`・`/interests/events`・`/interests/papers`)は「選んだトピック+配下」の
  ものだけを出し**(選択の外は消さずに表示から外れるだけ)、
  トレンド側(`/recent/general`・`/recent/news`・`/recent/articles`・`/recent/papers`)と定番
  (`/classics/books`・`/classics/events`)は**絞らずに強調だけする**(★とタグの色)——
  選択に依らない軸なので一覧は減らさず、自分の関心に当たるものが見つかればよい。
  **興味トピックのサマリーの材料**(`DigestRunner` の `DigestScope.Interests`)も
  この範囲で選ぶ(全体のサマリーはトピックに依らない)。**画面の説明文(`/settings/topics`)もこの 2 つの効き方で書くこと** ——
  「選んだトピック = 収集対象」「選んだトピック+配下 = 強調(トレンド・定番)・
  サマリーの材料・興味トピックの一覧」
- **TECH PLAY の取り込みフィルタ(`EventCollectionRunner`)も配下込み。** TECH PLAY は
  検索できない RSS の巡回で、絞りは保存するかどうかの判定にしか使わない —— 検索語を
  広げない理由(リクエスト数)が当てはまらず、明示選択だけで絞ると表示対象のはずの
  配下トピックのイベントが保存されなくなる
- `wwwroot/topic-tree.js` に**チェックを配下へ広げる cascade は無い**(かつてはあった)。
  保存が広げなくなったので、画面でチェックが広がって見えると保存結果と食い違う

そのため **`GetTopicsAsync` は選択済みを話題度によらず先頭に固定する**。行を消さないだけでは
足りない —— 再収集で現れなかったトピックは話題度が 0 になり、上位 N 件から押し出されて画面から
消える。消えた状態で「選択を保存」(丸ごと置き換える側)を押すと、その選択まで外れて収集が止まる。

EF 版のタグ関連クエリだけ生 SQL にしている。`Tags` 列には値変換をかけていて LINQ から
翻訳できず、タグごとの件数集計には PostgreSQL の `unnest` が要るため。

### 正規化でやること・やらないこと

`TagNormalizer` が潰すのは**機械的な表記ゆれだけ**。

- NFKC で全角英数と半角カナをそろえ、小文字化する(`ＡＩ` と `AI`、`ｼﾞｪﾈﾚｰﾃｨﾌﾞ` と `ジェネレーティブ`)
- 区切り(空白・`-`・`_`・`・`・`/`)を落とす(`Claude Code` と `claudecode` を同じキーにする)。
  **`.` `#` `+` は残す** —— 落とすと `.net` が `net` に、`c#` が `c` になって別の語と衝突する
- **カンマ(`,`・`、`)は区切りとして「分ける」** —— 落とすと `a,b` が `ab` という別の語になる。
  収集元のタグ名にカンマが入っていることが実際にある(実測: Qiita の直近 100 記事のタグ
  346 個のうち `SEOツール,`・`AI活用,`・`コスト重視,` の 3 個)
- **先頭の `#` と、前後の `*`・`。`・末尾の `.` は飾りとして落とす。** 位置で扱いを変えるのが要点 ——
  先頭の `#` はハッシュタグの印(実測: Qiita のタグに `#生成AI`・`#プログラミング` が 7 個)で、
  残すと同じ話題が `生成ai` と `#生成ai` に割れる。**語の中の `#` は残す**(`c#`)。
  `*` は Markdown の強調が漏れたもの(実測: `**Video` というタグ名)。
  **先頭の `.` は落とさない**(`.net` が `net` になる)
- **トピックでない語(ストップワード)を落とす。** 収集元の分類名や読み手の行動を表す語
  (`テクノロジー`・`あとで読む`・`初心者`)。実データでは 772 タグ中の上位 5 件のうち 2 件が
  これで、残すと話題度の上位を占めてトピック一覧が使い物にならない

**やらないこと**が 2 つある。`ai` と `人工知能` のような**同義語は機械的に潰せない**ので
別名カタログの仕事にする。`ai` ⊃ `生成ai` ⊃ `llm` は同義ではなく**粒度の違い**なので、
そもそも統合しない —— まとめると上位の語だけが巨大化して、何の話題か分からなくなる。

### 未知の語の LLM 分類(`ITopicClassifier`)

カタログに無い語は、**「タグを仕分けなおす」の中で LLM が分類してツリーへ入れる**。
**候補は記事・イベント・書籍の収集で自然に溜まる**(見かけたタグが `Tags` に `Pending` で
入り、`/settings/tags` の「仕分けまち」にも出る)—— 候補集め(語彙)と話題度(鮮度)は
性質が別で、候補のために外部へ聞きに行く必要はない。同義語・粒度の違いの判定は語の意味を知らないとできないため、
機械的な正規化やカタログでは扱えない領域をここが受け持つ。方式は要約と同じ 2 つ
(Claude Code のトークン優先、無ければ Anthropic API キー。両方無ければ分類なしで
収集だけ動く)。指示文とスキーマは `TopicClassificationPrompt` に集約。

- 1 語ごとに **alias(既存トピックの同義語)/ new(親付きの新トピック)/
  skip(トピックにしないと確信)/ unknown(知らない・新しすぎる)** の 4 択で返させ、
  `TopicClassificationValidator` が検証する —— 実在しない寄せ先・自分自身への寄せ・
  実在しない親は捨てる(**LLM の応答をそのまま信じない**)
- 通った仕分けは **`Tags.Status` に、新トピックは `Topics` に保存する**。
  起動時は DB からカタログを組み直すので、コンテナを作り直しても語彙は残る
- **除外(`NotTopic`)も保存する** —— 保存しないと、トピックでない語を毎回
  LLM に聞き直して枠を無駄にする。
  **判断できなかった語(`Unresolved`)は期限付き**(`RetryAfter` = 7 日後。
  期限を列に持つので「7 日」の計算が読む側から消える)。
  保存しないと毎回同じ語を聞き直して枠を無駄にし(実測: 毎回 ~20 語が再出題されていた)、
  無期限に確定させると、LLM の知識に無い新語(まさにツリーに入れたい語)が永久に平置きのまま残る
- **タグの再正規化は仕分けの中で毎回、分類の前と後の2回走る**。数秒で冪等な DB 処理なので
  2 回でよく、それぞれ役目が違う:
  **前** —— 正規化の規則やカタログを変えたときに<b>そこで初めて現れる語</b>
  (`aiと機械学習、データエンジニアリング` を 2 語に分けた等)を、その回の分類対象に含める。
  後段だけだと、その語は次に押すまで分類されず「押しても処理されない」ように見える。
  **後** —— 今回の分類で増えた別名を過去データへ反映する。
  かつての「タグを再正規化」ボタンはこれに吸収して消した
- **対象は `ITagStore.GetPendingAsync` が決める**(状態が `Pending`、または `RetryAfter` を
  過ぎた `Unresolved`)。`/settings/tags` の「仕分けまち」と仕分けの入力が<b>同じ 1 か所</b>から
  出るので、押す前に何が流れるかが分かる(`/settings/topics` の案内の語数も同じところから数える)
- **件数で足切りはしない**(`GetPendingAsync` に下限は無い)。**画面の「仕分けまち」が
  そのまま対象**になるようにするため —— 以前は 3 件未満を落としていたが、
  **収集ボタンを何回押したかで対象が変わる**うえ、落ちた語は仕分けまちに残り続けた。
  代わりに並びを「件数 + 話題度」の大きい順にして、1 回の枠(300 語)に収まらない分を
  次の回へ回す。**ゴミは `/settings/tags` の「チェックした語を除外する」で LLM に聞かせる前に落とす**
- **仕分けは外部トレンドを引かない。** だから**押しても仕分けまちは増えず、押すぶんだけ減る**
  —— トレンドを同じジョブで引いていた頃は、その回に取得した語がそのまま対象に足されて
  仕分けまちが尽きなかった(実測で「候補 1 語のはずが 73 語聞いていた」)。
  新しい語が入るのは収集と「話題度を取り直す」のとき
- 1 回の実行で渡す語は 300 個まで(`MaxTagsPerClassification`。呼び出し回数の暴走を防ぐ枠。
  積み残しは次の実行で続きから)。**LLM への呼び出しは 60 語ずつのバッチ**(`BatchSize`)——
  200 語を 1 回に詰めたら応答の生成が 300 秒のタイムアウトを超えて丸ごと失敗した(実測)。
  後続バッチが失敗してもそれまでの分類は保存する
- **除外と確定した語は語彙に入らない**(タグとしては `NotTopic` で残る)。
  ニュース・開発 のような一般語が話題度の上位を占めると一覧が読めないため
- **同義のトピックは統合パスが寄せる**(`ITopicMergeAdvisor`。仕分けの手順 3)。
  分類の検証はキーの重複しか見ないので、あるバッチが `AI` を、別のバッチが `人工知能` を
  新トピックとして作りうる —— **後から寄せる手当てがあるので、語彙を空から始められる**
  (初期値の JSON を捨てられる)。一覧は**バッチに割らず 1 回で渡す**(重複がどこにあるか
  分からないので、割ると跨いだ重複を見落とす)。応答は検証してから適用する:
  実在しない寄せ先・自分自身・**相互参照(A→B と B→A)は捨て**、連鎖(A→B→C)は最後まで辿る
- **話題度は「単体」と「配下込み」の 2 列を持つ**(`Topics.TrendScore` /
  `SubtreeTrendScore`。単体は「自分自身 + 別名」のタグから合算し、配下込みは
  さらにツリーを畳み込む)。「プログラミング言語」のような構造の語は単体の
  話題度がほぼ付かず、単体だけで並べると一覧の取得件数から押し出されて子が根として孤立する。
  一方、合算だけだと汎用的な親ばかりが上がる —— だから両方持って使い分ける。
  **取得の足切りとツリーの並びは合算、ランキング表示は単体**
- `/settings/topics` の一覧は**ツリー(既定)とランキングの2表示**を `?view=ranking` のリンクで
  切り替える(静的 SSR のまま)。切り替えは**タブの見た目**(下線を1本敷き、選択中だけ
  面とつなげる)。タブの下の1行に検索(左)と選択の説明(右)を並べる。
  **「ファイルで持ち出す / 取り込む」はページの一番下**(整備のボタンは設定へ移した)——
  毎回使うものではなく、一覧より先に目に入ると「まず押すもの」に見えてしまう。
  「話題度を取り直す」の説明(手順・話題度の算出方法)は **`?` を押して出るチップ**
  (`popover`。一覧の説明チップと同じ流儀だが、こちらは PC でも出す)にまとめてあり、
  **副作用(仕分けまちが増える)だけはチップに隠さずボタンの右に書く** ——
  押す前に知っておくべき話のため。**どちらも設定側へボタンごと移動している**。**話題度では足切りしない**(0 でも消さない)——
  親を選べば配下も興味の範囲(表示・強調)に入るので、配下が見えていないと
  何を選んだのか分からない。
  代わりに**出すのは「語彙(カタログ)の正式表記」と「選択済み」だけ**
  (`TopicCatalog.IsTopic`。取得は `ITopicStore.GetAllAsync`)。
  **選択済みを必ず残すのは、保存が「一覧に出ている分で置き換える」ため** ——
  隠すと保存のときに選択ごと外れる。
  応答圧縮を入れてあるのもこのページのため(`Program.cs` の `AddResponseCompression`)。
  ツリーは `details`/`summary` で**子を折りたためる**。
  **既定は「根は畳む・中は開く」**(`open="@(depth > 0)"`)—— 根まで開くと 300 行が縦に並んで
  領域の見分けがつかず、中まで畳むと目的の語に届くまで何度も開くことになる。
  **`wwwroot/topic-tree.js` が覚えるのは既定との差分だけ**(タグ → 開/閉)。
  「閉じたノードの一覧」を持つ作りだと、既定で畳んである根を毎回開いてしまう
  (nav-menu.js と同じ流儀で localStorage に持ち、enhanced navigation 後にも当て直す)。
  閉じていてもチェックボックスは DOM に残るので、選択の保存(見えている分で置き換え)は
  そのまま成立する。見た目はテーブル型 —— 各行を同じ列構成のグリッドにして列を縦にそろえ、
  字下げは名前セルの中だけ、開閉の三角は標準マーカーを消して自前の `.caret` で描く
  (コンテナごと下げたり標準マーカーを残すと、話題度・日時の列がずれる)
- 行にはトピック名のほかに**紐づくアイテム数**(`Topic.TotalCount` をラベル無しの
  かっこ書きで。0 のときは出さない —— ほとんどの行に (0) が並ぶと件数の付いた行を
  見つけられない)と**用語の一言説明**を出す。**「記事 N」と書かない** —— この数は
  記事だけでなくニュース・論文・イベント・書籍の合算なので、ラベルを付けると嘘になる
  (内訳はツールチップ)。`/settings/tags` の一覧も同じ見せ方
- **説明の見せ方は画面幅で変える。** PC はかっこ書き(溢れは `…`、全文は `title`)、
  **スマホは `?` を押すと出るチップ**(HTML 標準の `popover`。タッチ端末に hover が無く、
  かっこ書きを2行目に回すと 1000 行超の一覧が縦に伸びすぎる)。位置は画面下に固定 ——
  行の隣に出すには CSS のアンカー位置指定が要り、対応がまだ狭い。
  `?` は `summary` の中にも置くので、**クリックの伝播を `topic-tree.js` で止める**
  (止めないとチップを出すついでに折りたたみまで動く)

**残骸の行は仕分けが掃除する。** 正規化の規則を変えると、以前のキーの行
(`#生成ai`・`,` など)が幽霊として一覧に残る —— 中身は正しい行に合流済みなのに、
`UpsertAsync` は行を消さないため。仕分けは次の 2 種類を `RemoveAsync` で消す
(**選択済みは消さない**):

- **いまの正規化では作られないキーの行**(`TagNormalizer.Normalize([tag])` が
  `[tag]` にならない行)。**`ToKey` の比較では足りない** —— カンマは `Normalize` の側で
  語を分けているので、`生成ai,` は `ToKey` を通しても変わらず、判定をすり抜ける
- **表示名が空の行** —— `Display` 列より前に書かれた古い行(実測で 58 件残っていた)。
  以後の更新では必ず値が入るので、空であること自体が「更新されていない」印になる

### トピックの検索

タブの下の行の右側に検索ボックスを置いてある(見出しは持たない。何の検索かは placeholder と
aria-label で示す)。**GET のフォーム**(`?q=`)なので静的 SSR のまま動く。

- **別名でも当たる**のが要点(`人工知能` で引いて `AI` に当てる)。突き合わせは
  **正規化したキーどうしの部分一致** —— 表記ゆれを吸収したいので生の文字列では比べない。
  入力が別名そのもののときは `Resolve` で正式表記へ寄せてから当てる(`TopicCatalog.AliasesOf`)
- **当たった枝は開き、先頭の一致に `autofocus` を置く。** JS を使わずに「開いてフォーカス」を
  成立させるため —— ブラウザが読み込み時に焦点を移し、画面内までスクロールする。
  焦点は<b>入力そのもの(別名なら寄せた先)を優先</b>し、無ければツリーの並びで最初の一致
  (`ContainsMatch` は祖先にも立つので、祖先に焦点を移さないよう一致集合で確かめる)
- **検索で開いた枝には `data-force-open` を付ける。** `topic-tree.js` の復元に勝たせるためで、
  付けないと以前に畳んだ枝が閉じ直されて検索結果が隠れる
- **絞り込みはしない**(一致した行を強調して開くだけ)。周りの語との関係が見えたほうが、
  収集対象を選ぶ判断に使える
- 一覧に無い語は<b>用語集の側も見て案内する</b> —— 「なぜ出てこないのか」が分かるようにするため

### タグの画面と手直し(`/settings/tags`)

**見かけたタグを状態ごとに全部出す**(仕分けまち / 保留 / 除外 / 別名 / トピック)。
語彙に入らなかった語を画面から消すと、誤判定や仕分けの遅れに気づけないため。

**ここで仕分けを手で直せる**(`DecidedBy.Human` で記録)。これが `topic-seed.json` を
手で編集する代わり —— 権威が DB に移ったので、JSON を直しても投入済みの語彙には効かない。

- **タグ名は詳細ページ(`/settings/tags/<キー>`)へのリンク。** トピックの詳細と同じ作りで
  **ツリー構造だけ無い**(タグは語彙になる前の語なので、同義語も親子もまだ持たない)——
  代わりに上段が仕分けの状態・出どころ・件数に答え、下段は同じく記事・イベント・書籍。
  **別名のタグは一覧が空になる**(再正規化がデータのタグを寄せ先の表記で持ち直すため)ので、
  そのことを状態ブロックが説明して寄せ先の詳細へ誘導する
- **画面の見出しは「除外」**(`TagStatus.NotTopic`)。「トピック外」だと<b>無視する語だと
  読み取れない</b>ため画面の言い方だけ変えてある。**列挙子の名前は `NotTopic` のまま** ——
  DB に状態名の文字列で入っているので、改名するとデータの移行が要る
- **まとめて除外できる**(`ExcludeSelectedAsync`)。行ごとのチェックボックスと
  「チェックした語を除外する」で、**LLM に聞かせる前にゴミを落とす** ——
  仕分けまちの語を全部 LLM に流すようにしたので、間引く経路がここに要る。
  「この一覧を全選択」だけは JS(`wwwroot/tag-select.js`)で、**動かなくても
  1 語ずつのチェックと除外は素の HTML で成立する**
- **操作はフォーム 1 つで受ける。** 行ごとにフォームを作ると `@formname` が数百個になるので、
  押した行は `<button name="Action" value="promote:<キー>">` の値で見分ける
  (一括は `value="exclude-selected"` で、対象は同名の checkbox `Selected`)
- **別名と昇格は一段進める**(`?alias=<キー>` で寄せ先、`?promote=<キー>` で親を選ぶ
  フォームを出す)。行ごとに選択肢を置くと 1000 行のページに `select` が 1000 個並ぶ。
  候補は `datalist`(2つのフォームで共有)で打ちながら絞れる
- **昇格の親は選ばなくてもよい**(空なら最上位)。LLM の分類は親付きで語彙に入れるので、
  手の昇格だけ親を選べないと平置きの語が増える一方だった。親は別名でも書ける
  (`人工知能` → `ai` に解決)。**実在しない親は受け付けない** —— 黙って最上位に落とすと、
  選んだつもりの親が効いていないことに気づけない。既に語彙にある語なら親だけ付け替える
  (表記・説明は消さない)
- **トピックだったタグを別名にするのは「統合」**(`TopicMerger`)。寄せ元を指すタグの
  付け替え・配下の親の付け替え・寄せ元の行の削除まで面倒を見る。
  **収集対象に選ばれているトピックは寄せない**(収集キーワードが黙って変わるため)
- **除外・仕分けまちへ戻すときは語彙の行も消す**(`TopicMerger.DemoteAsync`)。
  残すと、どのタグにも紐づかない行がツリーに居座る

### 用語の一言説明(`ITopicDescriber`)

見慣れない語が一覧に並んだときに何の話か分かるよう、トピックは**一言説明**を持てる
(一覧はトピック名のツールチップ、詳細ページは見出しの下)。置き場は語彙と同じ2層で、
`topic-seed.json` の `description`(人が書いた説明)が **DB の `TopicDescriptions`
(LLM が埋めた説明)より優先される**(`TopicCatalog.ApplyDescriptions`)。

**LLM の呼び出しを増やさないのが設計の要点。**

- **新しく見つかった語の説明は分類の応答に相乗りさせる** —— 分類のスキーマに
  `description` を足しただけなので、追加の呼び出しは無い(出力トークンが少し増えるだけ)
- **既にカタログにあって説明だけ無い語は、仕分けの中で埋める**(`DescribeMissingAsync`)。
  1 回の実行で 300 語まで・60 語ずつのバッチで、**1 語につき 1 回しか聞かない**
  (結果は DB に残る)。上限で切れる分は**収集対象に選んだトピック → 話題度順**で先に埋める
- **説明させるのは正式表記**(`生成AI`)。キー(`生成ai`)は小文字で区切りも落ちていて語として読みにくい
- **長さは指示だけに任せない**(`TopicDescriptionPrompt.Trim` が 120 文字で切り、改行を畳む)。
  超えた応答をそのまま持つと一覧のツールチップが読めない
- **知らない語は空文字で返させ、空は捨てる** —— 推測で書かせるとカタログが嘘を持つ。
  空だった語は次回また聞く(分類の Unknown と違い期限は持たない。説明は無くても実害が無いため)
- 分類器と説明者は**同じインスタンス**(`ClaudeCodeTopicClassifier` / `AnthropicTopicClassifier`
  が両方の抽象を実装し、`Program.cs` が 2 つの抽象として配る)。方式の選び方も分類と共通

### 語彙の持ち出しと取り込み(`TopicExporter` / `TopicImporter`)

**LLM の仕分けは呼ぶたびに枠を使う**ので、本番で仕分けた語彙を開発サーバーへ持って行ける
ようにしてある(逆向きにも、バックアップにも使える)。画面は `/settings/topics` の
「ファイルで持ち出す / 取り込む」。

- **運ぶのは語彙(`Topics`)とタグの仕分け(`Tags` の状態)だけ。** 件数・話題度・
  最初に見た時刻といった**観測は入れない** —— あれは「その環境が集めたデータ」の話で、
  持ち込むと取り込み先の実データと食い違う(件数は次の整備が集め直す)
- **未仕分け(`Pending`)のタグも持ち出さない**(まだ何も決まっていない)。
  逆に **`NotTopic` と `Unresolved` は持ち出す** —— 運ばないと、取り込んだ側が
  同じ語を LLM に聞き直して枠を無駄にする
- **書き出しは `GET /export/topics.json`**(最小 API)。ダウンロードは GET 1本で足りるので
  Blazor のフォームにはしない。**`/settings/topics/…` の下に置かない**のは、トピックの詳細
  (`/settings/topics/<タグ>`)と紛れないようにするため
- **取り込みは `/settings/topics` の multipart フォーム。** ファイルは
  `HttpContext.Request.Form.Files` から読む —— **静的 SSR のフォーム束縛は
  ファイルを扱えない**(`[SupplyParameterFromForm]` に `IFormFile` は来ない)
- **取り込みはジョブにしない**(`JobRunner` を通さない)。外部へは出ず DB の数回の
  書き込みで終わるので、押した人がその場で結果を読めるほうがよい
- **足し込みで、消さない。** 取り込む側にしか無い語彙・仕分けはそのまま残す。
  **画面から人が直した仕分け(`DecidedBy.Human`)はファイルで上書きしない** ——
  LLM より人の判断を優先するのは画面の手直しと同じ規則
- **件数を消さないために、`UpsertAsync` には既存の行も全部渡す** ——
  渡されなかった行は件数と話題度が 0 になるため
- **判定日時(`DecidedAt`)は持ち出し元のものを保つ**(時刻ごとにまとめて `DecideAsync`)。
  全部を取り込んだ時刻にすると、「同じ実行で付けた分類は時刻が揃う」を頼りに
  「前回聞いた語」を復元している画面が、取り込み全体を 1 回の分類として出してしまう
- **収集対象の選択は既定で取り込まない**(チェックボックスで明示したときだけ)——
  イベントと書籍の検索語が黙って変わると、問い合わせ先が勝手に増減する
- **ファイルを信じない。** 実在しない親と自分自身への親は落として最上位にし、
  寄せ先の無い別名は捨てる(手で編集もできるし、循環したままだとツリーを描く側が
  延々とたどる)。壊れた JSON は例外にして**理由を画面に出す** ——
  黙って 0 件にすると「取り込んだのに増えない」で原因が分からない
- 取り込んだあとは `TopicCatalogRefresher` でカタログを組み直す

### 生タグを保存する(`RawTags`)

記事・イベント・書籍は、収集元から受け取ったままのタグを `RawTags` に持つ。
**正規化後の値しか持たないと、規則を変えても過去のデータに反映できない**ため
(`Tags` が `init` ではなく `set` なのも、ここから作り直すため)。

規則を変えたら設定の「タグを仕分けなおす」を実行する(仕分けが毎回
`TagRenormalizationRunner` を回す)。`RawTags` から `Tags` を作り直すだけで外部へは
出ないので、何度走らせても同じ結果になる。

`RawTags` を足したマイグレーション(`AddRawTags`)は、**既存行の `RawTags` を当時の `Tags` で
埋める**。元の表記はもう取り戻せないので、手元にある値で埋めて再正規化が空にならないように
してある。以降に収集した行には収集元の値がそのまま入る。

## 書籍収集

集めたいのは新刊ではなく**その分野で読んでおくべき本**。検索は関連度順で引き、
並びは「どのくらい読まれているか」(レビュー)で決める。

**openBD はキーワード検索を持たず ISBN 参照専用**なので、役割を分けている。

- キーワード検索は Google Books API(`IBookCatalog`)。**API キーは実質必須** ——
  キー無しのリクエストは Google 共有の匿名プロジェクトの枠に入り、その枠は1日あたり 0 件
  (`defaultPerDayPerProject` が 0)なので、最初の1回から 429 が返る。
  **`orderBy` は付けない**(既定の関連度順)—— 新着順は取りこぼしが大きく、実測で
  `機械学習` が 0 件(関連度順なら 300 件)だった
- openBD は検索結果の書誌情報を ISBN で補う後段(`IBookEnricher`)。
  **既に値がある項目は上書きせず、欠けている項目だけを埋める**
- 楽天ブックスは**レビュー**の後段(`RakutenBooksEnricher`)。書誌情報は触らない ——
  **例外は書影**(下記)

### 雑誌・ムックは入れない(`Periodical`)

集めたいのは**その分野で読んでおくべき本**なのに、キーワード検索の結果が雑誌に埋まっていた
(実際に週刊アスキーの号が並んだ)。雑誌は**号ごとに別の本として出てくる**うえ、レビューも
推薦も付きにくいので、並べ替えでも沈まずに一覧を占める。落とし方は 2 段:

- **Google Books の検索に `printType=books` を付ける**(公式の絞り込み。追加コストなし)
- **タイトルの型で落とす**(`Periodical.IsLikely`)—— `printType` が弾くのは Google が
  雑誌として登録しているものだけで、**日本のムック・別冊・増刊は ISBN 付きの書籍として返る**。
  見るのは「週刊 / 月刊 / 隔月刊 / 季刊 / 増刊 / 別冊 / ムック / MOOK / 総集編」と
  **号数の型**(`2026年9月号`・`9月号`)。**迷う語は入れない**(`Vol.` は巻数を持つ書籍もある)
  —— 読むべき本を取りこぼすほうが害が大きい

効かせる場所は**収集と表示の両方**:

- 収集(`BookCollectionRunner` / `ClassicsCollectionRunner`)は**保存する前**に落とす。
  **補完より前**に落とすのは、捨てると決めた本で openBD や楽天を叩かないため
  (定番だけは補完の後 —— タイトルが入るのが補完だから)。落とした数はログに出す
- 表示(`/interests/books`・`/classics/books`)でも外す。**規則を入れる前に集めた本が DB に
  残っている**ので、表示で外さないと直したことが画面に出ない。
  画面にも「雑誌・ムック・増刊は出していません」と書いてある

### 書影(`CoverUrl`)はどこから来るか

**openBD は技術書の書影をほとんど持っていない**(実測: リーダブルコード・達人プログラマー・
リファクタリング・Clean Architecture・SQL アンチパターン・Web を支える技術・人月の神話・
テスト駆動開発・ハッカーと画家・MySQL 徹底入門 の 10 冊すべて `cover` が空)。
興味トピックの書籍に表紙が出るのは Google Books の**検索結果**(`imageLinks`)が
書影を持っているためで、**ISBN からしか起こせない定番の書籍は表紙の出ない一覧になっていた**。
補完は 3 段で、**どれも書影が欠けているときだけ**埋める:

| 順 | 誰が | 追加のリクエスト |
|---|---|---|
| 1 | openBD(`OpenBdEnricher`) | 無し(書誌の補完と同じ応答)。ただし技術書はほぼ空 |
| 2 | 楽天ブックス(`RakutenBooksEnricher`) | **無し** —— レビューと同じ応答に `mediumImageUrl` が入っている |
| 3 | Google Books(`GoogleBooksCoverEnricher`) | **1 冊 1 リクエスト**(ISBN の一括指定ができない) |

- **3 は最後に置く**(`Program.cs` の登録順がそのまま実行順)—— 1・2 で埋まったぶんは引かない
- **保存の合流(`BookMerge`)で、欠けている書影だけ埋める。** 既にある本は書誌情報を
  上書きしない規則だが、**書影まで捨てると補完の追加より前に保存した本には永久に表紙が付かない**
  —— 収集のたびに Google Books へ問い合わせては保存前に捨てる、という無駄も続く
  (`Book.CoverUrl` が init ではなく set なのはこのため)。**既に値があれば上書きしない**
- **同じ本を毎回引かない。** 定番の収集は ISBN からまっさらな `Book` を組み立てるので、
  そのまま渡すと**毎回全冊ぶん**問い合わせることになる(無料枠は 1 日 1,000 リクエスト)。
  `ClassicsCollectionRunner` が**保存済みの書影を引き継いでから**補完へ渡す
- **冊数の上限は設けない。** 上限で切ると「何冊ぶん諦めたか」が画面に残らないまま、
  いつまでも埋まらない本が出る。代わりに間隔を空ける(`Books:CoverLookupDelaySeconds`。既定 1 秒)
- **枠切れでも、そこまでに取れた書影は保存する。** 残りを諦めるのは同じ(叩き続けても
  同じ応答が並ぶだけ)だが、**例外で抜けてはいけない** —— 呼び出し側は補完前の本を保存するので、
  数百リクエストぶんの書影が毎回消えて**いつまでも埋まらない**(推薦本は 860 冊で、
  無料枠 1 日 1,000 リクエストなので枠切れは必ず途中で起きる)。埋めた冊数はログに出す
- **枠切れは 429 だけではない。** Google の API は 1 日あたりの上限を
  `403 dailyLimitExceeded` で返すことがある(429 は短時間に送りすぎたとき)。両方で諦める
- **1 冊の失敗では止めない**(見つからない本・一時的なエラー)。ただし 5 回続けて失敗したら
  打ち切る —— 相手が落ちているのに数百回投げ続けないため
- **クレジット表記の条件が変わる。** 楽天由来のデータは**レビューだけではなくなった**ので、
  判定は `RakutenCredit`(レビュー件数 > 0、または書影のホストが楽天)。
  **書影の出どころを列に持っていない**ので URL のホストで見分ける —— 表示のたびに読む値なので、
  実際に画面へ出るものと食い違わない

### 薦められている度合い(`IBookRecommendationSource`)

「読むべき技術書」を挙げた記事から、薦められている本を拾う(`QiitaBookRecommendationSource`)。
**レビュー(読まれた量)とは別軸**で、こちらは「詳しい人が名指しで薦めたか」。並びでは
**推薦回数を優先**する —— レビュー数は一般向けの本ほど有利になるため。

- **公式 API(v2)を使う。** 検索が本文まで返すので記事ごとに引き直さない。未認証 60/時、
  トークンありで 1000/時。`Qiita:Queries` は**ストック数の下限**で絞る(読まれていない記事の
  推薦まで数えると指標が薄まる)
- **クエリは複数で、1クエリずつページングして読む**(1ページ 50 件、`Qiita:MaxArticles` まで)。
  検索は新着順に返るため、1ページで打ち切ると古い定番記事が読めない(実測: `tag:技術書
  stocks:>100` は 52 件あるのに旧実装は新着 20 件しか読んでいなかった)。タグ検索だけだと
  タグ無しの「読むべき本」系記事を取りこぼすので**本文検索のクエリも混ぜる**(実測 374 件)——
  ノイズは ISBN の検算が落とすので当たりの広さは害にならない。同じ記事が複数のクエリに
  当たっても URL の重複を落として1票
- 本の特定は**記事に貼られた Amazon リンクの ASIN**。書籍の ASIN は ISBN-10 そのものなので
  ISBN-13 に直せる(`Isbn.FromAsin`)。**チェックディジットまで検算する**ので、`B0…` で始まる
  Kindle 専売や電子機器は落ちる(実測: 90 個中 82 個が書籍)
- **保存するのは ISBN と出典記事の URL だけ**。記事本文は保存しない(複製にしないため)。
  画面では**出典を全部たどれるようにする**(`BookItem` の番号リンク)—— 1 本目だけを
  リンクにしていた頃は、「3 件」と数だけ見えて残りへ行けなかった。記事の題名は
  持っていないので番号で並べる
- 拾えるのは ISBN だけなので、**タイトルは openBD が埋める**。`OpenBdEnricher` がタイトルの
  空欄だけを埋めるのはこのため。埋まらなかった本は保存しない(空行が並ぶだけになる)

### 読まれている度合い(`BookPopularity`)

`ReviewCount` / `ReviewAverage` から「読んでおくべき度」を出す。**評価をベイズ平均で件数に
応じて割り引き、件数の対数を掛ける** —— 件数だけだと評価の低い話題書が定番書を押しのけ、
評価だけだとレビュー 1 件で星 5 の本が最上位に来る。

- **`ReviewCount` の null と 0 は別物。** null は「取れていない」、0 は「読まれていない」。
  混ぜるとアプリ ID を設定する前に集めた本がまとめて最下位に沈む。`Score` も null を返す
- 楽天は**レビューが無いとき `reviewAverage` に `"0.0"`** を返す。そのまま平均に使うと
  評価が最低の本になるので、パーサが 0 を null に落としている
- **ISBN の一括指定ができない**(openBD と違う)ので 1 冊 1 リクエスト。既定 1 秒間隔
- **楽天から取ったデータを画面に出す場所にはクレジット表記が要る**(利用規約 Article 13)。
  指定の HTML は改変不可。判定は `RakutenCredit`(**レビューだけでなく書影も楽天由来のことがある**)で、
  テキスト版を `/interests/books` と `/classics/books` に置いてある
- Google Books の `ratingsCount` は日本語書籍にほぼ入っていない(実測 20 件中 0 件)ため使わない

**検索語は設定ではなく選択中のトピック**(`ITopicStore.GetSelectedAsync` の `Display`)。
1 キーワードずつ問い合わせ、検索に使ったキーワードをそのまま書籍のタグにする
(正規化前の値が `RawTags`)。設定に検索キーワードは持たない —— かつての `Books:Keywords` は
トピック選択に一本化した時点で使われなくなっていたので消した。

重複判定は `BookKey`(ISBN-13 →(無ければ)詳細ページの URL →(無ければ)タイトル)。
**既存の本は書誌情報を上書きしないが、タグとレビューは取り込む**(`BookMerge`)—— 書籍は
トピックごとに検索するので 1 冊が複数のトピックで見つかる。捨てると最初のトピックにしか
出てこない。レビューだけ上書きするのは、時間とともに増える数値だから(取れなかった回に
null で上書きはしない)。
`Book.RawTags` が記事・イベントと違って `init` ではなく `set` なのはこのため。
**新しく `Book` を組み直す箇所では `RawTags` を写し忘れないこと** —— 落ちても収集直後は
正常に見え、タグの再正規化が走った瞬間にその本のタグが空になる(`OpenBdEnricher` で
実際に踏んだ)。

設定は `Books` セクション(`DelayBetweenKeywordsSeconds` /
`UseOpenBd` / `MinReviewCount`)と `Rakuten` セクション(`DelaySeconds`)。
**キー(Google Books の API キー・楽天のアプリ ID)は設定ではなく外部連携の画面から入れる。**
`MinReviewCount` の足切りは**レビューが取れた本だけが対象**で、取れていない本は落とさない
—— アプリ ID の無い環境で 1 冊も保存されなくなるため。`GoogleBooksApiKey` が空でもジョブは登録する
(検索は毎回 429 で失敗する)。**ボタンごと消すより 429 の理由を画面に出すほうが打つ手が
分かる**ため、429 のときだけキー未設定かどうかを見分けたメッセージを投げている
(`GoogleBooksCatalog`)。

取り込むのは**書誌事実(タイトル・著者・出版社・刊行日・ISBN・リンク・書影 URL)だけ**で、
`description` や `textSnippet` といった出版社の著作物は取り込まない。書影は画像自体を保持せず
URL のリンクにとどめる。

`/interests/books` は**トピックごとの折りたたみ**(`details`/`summary`)。全ページ静的 SSR なので、
開閉に JS も対話回線も使わない。**グループは収集対象に選んだトピックの分だけ**で、
**配下のトピックのタグしか持たない本も親のグループへまとめる** —— 配下ごとに割ると、
親を 1 つ選んだだけで小さな見出しが何十個も並ぶ。どのタグでそのグループに載ったのかは
**本のタグの帯の強調**で示す(`BookItem.HighlightTags`。グループのトピック+配下に
当たるタグだけ青いバッジ、他は控えめのまま)。1 冊が複数のグループに出るのは意図した挙動
(親と子を両方選んでいれば両方に出る)。選択を外したトピックで過去に集めた本や
タグの無い本は表示から外れる(データは消さない。トピックに寄らない推薦本は
`/classics/books` の担当)。

推薦の見せ方は2段構え(`/settings/topics` の詳細ページも同じ):

- **推薦だけを並べる一覧(`/classics/books` 定番の書籍)は推薦 2 本以上だけ**
  (`MinRecommendations`)。1 本だけの推薦は、まとめ記事が網羅的に貼ったリンクも含まれて薄い
- **`/interests/books` のトピックの折りたたみの中では推薦 1 本から強調**(左帯 + 薄い面。記事一覧の
  「選んだトピック」と同じ文法)。こちらはトピックで既に絞られているので下限を設けない

## LLM 要約

要約の実装(`ISummarizer`)は2つあり、**実行のたびに `LlmGateway` がキーの状態から選ぶ**
(かつては起動時に環境変数を見て DI 登録を分岐していたが、それだと画面からキーを設定しても
再起動するまで効かない)。**キー・トークンは外部連携の画面から設定する**(環境変数では
渡せない)。**両方未設定でもジョブのボタンは消えず、disabled + 理由付きで出る**
(`JobButton` / `JobRunner.NotConfiguredReason`)—— 消すと機能の存在ごと画面から見えなくなる。
定期実行のワーカーもキーの有無を見ずに登録し、未設定の周回は何もしない(キーを入れた
次の周回から動き出す)。

| 方式 | 選ばれる条件(キーはどちらも画面から設定) | 課金 |
|---|---|---|
| `ClaudeCodeSummarizer` | Claude Code の OAuth トークンがある(**優先**) | サブスクリプションの枠 |
| `AnthropicSummarizer` | Anthropic API キーがある | API の従量課金 |

共通の設定は `Anthropic` セクション(`Model` / `BatchSize`)。指示文は
`SummaryPrompt` に集約し、方式を切り替えても要約の口調が変わらないようにしている。
定期実行の可否と手動実行は「定期実行と手動実行」を参照。

### Chiezo 方式(相手を画面で選ぶ)

**環境変数 `Chiezo__BaseUrl` を入れると、LLM の相手を画面(設定 → AI)から選べる。**
Chiezo(LAN 内の知識サーバー)は Gemini・Claude Code・推論サーバ…の認証情報を持っていて、
`GET /v1/ai/backends` で「話せる相手・モデル・エフォート」を、
`POST /v1/ai/complete` で素の問い合わせを受ける。**こちらは鍵を持たずに相手を選ぶだけでよい**
—— サイドカーの CLI ブリッジは Claude Code 1 つしか包めないので、相手を選べる経路はここだけ。

- **`/v1/chat` は使わない。** あちらは知識ベースを引いて答える口で、必ず抽出が混ざる ——
  こちらは材料もプロンプトも自前で持っているので邪魔になる(トークンも余分に使う)
- **既存の実装は変えずに済む。** `ChiezoAiBridge` が `ICliBridge` を実装するので、
  要約・翻訳・分類・ダイジェストは相手が CLI ブリッジでも Chiezo 越しの Gemini でも同じ
- **選択は DB に持つ**(`Ai:Config`。JSON 1 本)。相手・モデル・考える量の 3 つ組が
  何組も入るので、独自の区切り文字で組み立てるとモデル名に区切りが混ざったときに壊れる
- **メインは全部のジョブ、サブは今日のサマリーだけ。** 比べて読みたいのは文章で、
  要約や翻訳は枚数が多く読み比べられない(呼び出しも保存も相手の数だけ増える)
- **URL を入れただけでは切り替わらない**(メインを選ぶまでは従来の方式のまま)——
  相手が決まっていない状態で勝手に 1 つ選ぶと、どれで動いているのか画面から分からない
- **表示名は選んだ時点の表記を保存する。** 表示のたびに Chiezo へ問い合わせると、
  繋がらない日にホームの但し書きが消える
- **繋がらないときは画面のエラー文で3つに切り分ける**(`ChiezoAiClient` が出し分けている)。
  「繋がらない」＝TCP が張れない(IP 違い・落ちている)、「エラーを返した(HTTP 404)」＝届いている
  (URL に `/v1` を付けた、または相手が古い)、**「時間内に応答しなかった」＝パケットが落ちている**
  (ファイアウォール)。**同じホストで動かしていても、コンテナからホストの公開ポートへ戻る経路は
  塞がれていることがある**(NAS で実際に踏んだ)。そのときは chiezo のネットワークに相乗りして
  `http://chiezo-api:7010` を指す —— 手順と注意点(`default` を並べ忘れると db にも繋がらなくなる)は
  `docker-compose.standalone.example.yml` に書いてある
- **モデルを「相手の既定に任せる」で頼んだときは、応答が名乗ったモデルで生成者名を作る**
  (`ChiezoAiBridge` が直近の応答のモデルを覚える)。こちらは名前を送っていないので、
  何が書いたのかを知る手がかりはそれだけ。生成者名を付けるのは応答を読んだ後なので順序は満たされる

### Claude Code 方式(CLI ブリッジ経由)

Claude Code にログイン済みの端末で `claude setup-token` を実行して得た長期 OAuth トークンを
外部連携の画面から設定する。**ホストの `~/.claude` はマウントしない。**

**CLI はこのアプリのイメージに入っていない。** 別コンテナの CLI ブリッジ
(chiezo リポジトリの `chiezo-bridge`。Claude Code を OpenAI 互換の `/chat/completions` に
見せるサイドカー)へ HTTP で頼む(`CliBridgeClient`)。**CLI の実体だけで 100MB 超**あり、
版を上げるたびにこちらのイメージを焼き直すことになるため、更新はブリッジ側に任せる。

**トークンは共有ディレクトリの設定 DB で渡す**(`BridgeCredentialStore`。既定
`ClaudeCode:StateDirectory` = `state`、compose では `data/state`)。CLI は別コンテナで動くので、
このプロセスの環境変数では渡せない —— `provider_settings` 表に書き、ブリッジが**読み取り専用で
マウントして読む**。書くのは `LlmGateway` の組み立て(= キーの版数が変わったとき)で、
**ブリッジは要求のたびに読み直す**ので入れ替えても再起動は要らない。**表の形はブリッジとの約束**
(列を減らさない)で、**WAL にしない** —— 読み取り専用マウントでは `-shm` を作れず開けなくなる。
**画面からトークンを消したときも書く**(残すと古いトークンでブリッジが動き続ける)。

**ブリッジはホストへ公開しない。** 認証が無く、しかもトークンを読める —— 外から触れると
そのまま AI を使われる。呼ぶのは同じ compose ネットワークの `app` だけ
(手元の `dotnet run` から使うときだけ `docker-compose.dev.yml` が 127.0.0.1 に開ける)。

設定は `ClaudeCode` セクション(`BridgeUrl` / `StateDirectory` / `Model` / `TimeoutSeconds`)。
**モデルは CLI の既定に任せず `claude-sonnet-5` を明示している**(appsettings.json と
compose の既定)—— CLI の既定は重いモデル(実測 claude-fable-5)で、要約・分類の定型
ジョブにはサブスクの週間枠を消費しすぎるため。変えるときは `CLAUDE_CODE_MODEL`。
どのモデルが使われているかは設定画面のジョブ名(「記事の要約(Claude Code /
claude-sonnet-5)」)とホームのダイジェストの生成者名に出る。**LLM を使うジョブはすべて
同じ書き方でボタンに出す**(要約・論文タイトルの翻訳・今日のサマリー・**タグの仕分け**)——
押す前に「どの枠を使うのか」が分かるように。話題度の取り直しは LLM を使わないので付けない
(名前は `TopicMaintenanceRunner.ClassificationName` が組む)。

**呼び出し1回の固定費が大きい。** 記事1件だけ渡しても Claude Code のハーネスが毎回入力に乗る
(実測: 1件で 32,300 トークン、5件まとめて 1件あたり 6,584 トークン)。だから `ISummarizer` は
記事1件ではなく**リストを受け取る**形にしてあり、`BatchSize` の既定も 20 と大きめにしている。
ツールを禁じても固定費はほとんど減らないので、**まとめて渡すこと自体が唯一の対策**。

実装上の勘所:

- **スキーマはプロンプトで指示する**(`ClaudeCodeBatch.WithSchema`)。ブリッジは OpenAI 互換の
  口(テキスト応答)なので、CLI を直接起動していた頃の `--json-schema`(構造化出力)を
  載せる場所が無い。**強制ではなくなった**ぶん、応答から JSON を切り出して読む
- **前置きとコードフェンスを許して読む**(`ClaudeCodeResponseParser.ExtractJson`)。
  「JSON だけ」と指示しても説明を1行添えてくる応答はあり、そこで丸ごと捨てると
  1バッチ分の要約が消える。**形が違うときは例外にする** —— それらしいテキストを
  要約として紐づけないため。失敗したバッチはジョブが次の巡回で引き直す
- **読めなかったら1回だけ言い直させる**(`ClaudeCodeBatch.RunJsonAsync`)。実測で、
  材料が1件しかない日のダイジェストが「材料が少ないため作成できません」という**説明文**で
  返り、生成そのものが失敗した。言い直しでは「説明を書かず JSON だけ」「材料が少なくても
  その範囲で組み立てる」まで伝える。**2回目も駄目なら投げる**(崩れ続ける相手に何度も投げない)
- **番号と値の対応で返させる**。記事の Id(GUID)を LLM に写させない
- **長さの上限は気にしなくてよい**。CLI に直接渡していた頃は Linux の単一引数の長さ上限
  (MAX_ARG_STRLEN = 128KiB)を避けて標準入力に流していたが、HTTP の本文にその制限は無い
- **失敗はブリッジが HTTP で返す**。504 = 上限秒数で打ち切り(`TimeoutException` に写す)、
  401 = 認証情報を読めていない(共有ディレクトリのマウントか設定 DB を疑う)、
  502 = CLI が非ゼロで終了。**こちらの待ちはブリッジより 30 秒長くする** ——
  先に切れると「ブリッジが何秒で諦めたか」が分からなくなる
- **道具は使わせない**。ブリッジ側で組み込みの道具を塞ぎ、MCP も繋がない設定
  (`CHIEZO_BRIDGE_MCP_URL` を空)で動かす。渡すのは自前のプロンプトだけ

### Anthropic API 方式

Anthropic 公式 .NET SDK(NuGet `Anthropic`)経由で Messages API を呼ぶ。呼び出しの固定費が
小さいので記事ごとに1リクエスト。モデルは既定で `claude-opus-5`、コストを抑えたいときは
`Anthropic__Model` を `claude-haiku-4-5`(入力 $1 / 出力 $5 per MTok)に変えられる。

## 今日のサマリー(ダイジェスト)

ホームの「今日のサマリー」に、**収集済みの情報から LLM がまとめたダイジェスト**
(導入 + 数項目)を出す。方式の選び方・キーは要約と共通
(`IDigestComposer` を `ClaudeCodeDigestComposer` / `AnthropicDigestComposer` が実装)。
指示文・入力・スキーマ・応答の読み取りは `DigestPrompt` に集約。

**サマリーは2本に分かれている**(`DigestScope`)。1回の実行で両方を作り、
**ホームには2枚のカード、ntfy には2通**の通知になる。

| 範囲 | 材料 | 作られる条件 |
| --- | --- | --- |
| `Overall`(技術界隈全体) | 話題度上位の記事・ニュース・論文だけ。**トピックの選択に依らない** | 材料があれば常に |
| `Interests`(興味トピック) | 選んだトピック(配下込み)に当たる記事・これからのイベント | **トピックを1つ以上選んでいるとき**だけ |

- **1本にまとめない。** 「界隈で何が起きたか」と「自分の興味に何があったか」は読む目的が
  違い、混ぜると興味の話が全体の話に埋もれる。**指示文も範囲ごとに変える**
  (`DigestPrompt.SystemFor`)—— 材料だけ変えて同じ指示を使うと、どちらも
  「話題の総ざらい」になって読み分けられない
- **興味トピック未選択のときは作らない**(ホームにも枠を出さない)。空のサマリーを
  作っても読むものが無く、「未生成」の枠だけが残る
- **興味トピック側は、全体で触れた記事を除かない。** 別々のサマリーなので、
  自分の関心でも大きい話は両方に出てよい(1本だった頃は同じ記事を2つの節で
  渡すことになるので除いていた)
- **材料の選別は LLM ではなく `DigestRunner` がやる** —— 上の表の範囲に絞って渡す。
  全量を渡すとトークンを浪費するうえ、選別の基準(話題度・選択)はデータ側の知識なので
  コードに置くほうが検証できる
- **複数の AI に同じ材料で書かせて読み比べられる**(Chiezo 方式のときだけ)。
  サブに選んだ相手はメインと**同時に**書く —— 順に走らせると後ろの相手ほど時間が空き、
  途中で収集が走れば材料まで変わる。保存は全員ぶんで、**同じ回**(`Digest.RunId`)として
  寄せる —— 生成時刻で寄せると、今日失敗した AI の前日ぶんが今日のものと並ぶ。
  **通知とホームの既定はメイン**(`IsPrimary`)。ホームは**タブで切り替える**
  —— 縦に並べるとホームが AI の数だけ伸び、横に並べるとスマホで読めない。
  切り替えはラジオ + CSS(`:checked ~`)で、静的 SSR のまま JS を使わない
- **失敗の扱いは3段に分ける**(`DigestRunner`)。**どれか1つの LLM が落ちただけで
  全部を捨てない**が、**メインが落ちた回を成功にもしない**:

  | 落ちたもの | どうなるか |
  |---|---|
  | サブの AI | 何も起きない(比較のための1本が欠けるだけ) |
  | メインの AI | **その範囲だけ**。もう一方の範囲は作りに行き、書けたサブは保存して先頭を繰り上げ、通知まで済ませる。そのうえで**ジョブは失敗**(`DigestPrimaryFailedException`) |
  | 通知先(ntfy) | 生成は成功のまま |

  - **範囲(全体 / 興味トピック)は互いに独立させる。** かつては先に作る「全体」で
    例外が上がると2本目を試すことすらできず、**片方の失敗でその日のサマリーが全滅**していた
  - **生成は同時に投げるが、失敗は値で返す**(`ComposeAttempt`)。`Task.WhenAll` は
    1本の例外で待ち合わせごと投げるので、投げ返すと**同じ回に書けていたサブまで捨てる** ——
    読み比べ用の相手がいちばん要る「メインが落ちた日」に記録が何も残らなかった
  - **繰り上げ(`IsPrimary`)は保存より先に立てる。** 保存の後に立てても DB には残らない
  - **失敗の文言(= 例外のメッセージ)に、落ちた相手・理由・残ったものを全部書く。**
    画面には「失敗: 」を付けてこれだけが出るので、読んで打つ手が分かる必要がある
    (テストで全文を固定してある)
- **末尾に「誰が書いたか」を残す。** 画面はカードの下(`digest-meta`)、**通知は本文の最後に
  `— <生成者名>`**(`NtfyDigestNotifier`)。複数の AI に書かせていると、どれが書いたものか
  分からなければ読み比べにならない。**署名は切り詰めの後に足す** —— 長い日にだけ
  消えるのでは意味が無い。**AI 自身に署名させない** —— モデルは自分の ID を正しく
  名乗らないことがあるので、こちらが知っている事実(選んだ相手 + 相手が名乗ったモデル)を書く
- **材料が少ない日でも作らせる。** 項目数の指示は「材料に応じて 1〜6 項目」で、
  **材料が1件でも断らせない**(`DigestPrompt`)—— 「3〜6項目」と決め打ちしていた頃は、
  興味トピック側の材料が論文1件だけだった日に「材料が少ないため作成できません」という
  文章が返り、JSON として読めずに生成が失敗した(実測)。読めなかったときの言い直しは
  `ClaudeCodeBatch.RunJsonAsync` が受け持つ
- **応答の URL は材料に含めたものしか通さない**(`DigestPrompt.Read`)。LLM が作った
  URL を画面の `href` に出さないため(`WebUrl` と同じ発想の検証)。項目数も
  `MaxItems` で打ち切る(指示だけに任せない)
- **生成履歴は `Digests` テーブルに残し、ホームは範囲ごとに最新の1件を出す**
  (`IDigestStore.GetLatestAsync(scope)`。範囲をまたいだ「最新の1件」は使わない ——
  作られる件数が違うので、混ぜると片方が出てこなくなる)。生成時刻と方式を
  画面に出す —— いつの状況のまとめか分からないと鵜呑みにできない
- 定期生成は `/settings/general` の「定期実行」で時刻とチェックを設定する(既定は走らない)。
  **材料が揃ってから走るよう、順番の最後に置いてある**。手動ボタンも `/settings/general`。
  材料が 1 件も無いときは LLM を呼ばずに止まり、ボタンの隣にそう出る

生成したサマリーは **ntfy へ通知できる**(`NtfyDigestNotifier` / `IDigestNotifier`)。
接続先(ベース URL・トピック名・トークン)は**外部連携の画面から設定**し(トピック名は
同じ画面の「生成」で作れる —— `NtfySettings.GenerateTopic`。**ntfy.sh のトピック名は
知っていれば誰でも購読・投稿できる**ので、`RandomNumberGenerator` で 80 bit 相当を作り、
見間違えやすい文字は使わない)、通知は**トピック名があるときだけ**送る —— ベース URL は未設定なら `https://ntfy.sh` が既定
(`NtfySettings.DefaultBaseUrl`)。通知先は送信のたびに解決するので DI には常に登録され、
未設定なら送らずに済ませる(`NotifyAsync` が false を返し、結果の「通知しました」に
数えない)。**通知のオン/オフは接続先とは独立**で、設定画面(`/settings/general`)の
チェックボックスで切り替える(`Ntfy:Enabled`。既定は有効・接続先を消さずに止められる)。
`Ntfy__ClickUrl`(通知タップで開くホームの URL)だけは環境変数で渡す。

- **JSON publish(ベース URL への POST に topic を含める)を使う** —— タイトルを
  ヘッダ(X-Title)で渡すと非 ASCII に RFC 2047 エンコードが要るため。JSON なら
  日本語のタイトルをそのまま書ける
- **通知は範囲ごとに1通ずつ**(まとめて1通にしない)。**タイトルと絵文字で見分けられる
  ようにする** —— 通知面は本文が畳まれるので、「今日のサマリー・技術界隈全体」
  「今日のサマリー・興味トピック」と題名で分かる必要がある(`newspaper` / `dart`)
- **送るのは生成と逆順**(興味トピック → 全体)。**ntfy のアプリは新着が上に並ぶ**ので、
  最後に送ったものが一番上に出る —— 読みはじめが「技術界隈全体」になるようにこの順にしてある。
  **生成の順は変えない**(全体が先。途中で失敗しても価値の大きいほうが残る)ので、
  `DigestRunner` は**全部作ってから通知する**(画面に出す文言は生成順のまま)
- **通知の失敗で生成を失敗にしない**(保存の後に通知し、失敗はログと結果の文言に出す)——
  通知先が落ちていることと、ダイジェストが作れたことは別の話
- 本文は素のテキスト(導入 → 項目 → 出典 URL)で、ntfy のメッセージ上限
  (既定 4096 バイト)に収まるよう `MaxMessageChars` で切る。
  `Ntfy__ClickUrl`(任意)を設定すると、通知のタップでホームが開く

## 日時の表示(JST)

**人に見せる日時はすべて日本時間**。保存と比較は UTC のままで、**表示の直前だけ
`JapanTime`(Core)を通す**。`ToLocalTime()` と `DateTime.Now`、書式の直書きは使わない。

- **収集元の `DateTimeOffset` は元の時差を持ったまま入ってくる。** そのまま書式化すると
  1つの一覧に `+00:00` と `+09:00` と `-05:00` が混ざる(実際そうなっていた)
- **`ToLocalTime()` は実行環境の TZ 次第**で、compose(`TZ=Asia/Tokyo`)と開発ホストで
  表示がずれる。`TZ` はログを読みやすくするために渡しているだけで、**画面の正しさを
  そこに依存させない**
- 時差は `TimeZoneInfo` ではなく**固定の +09:00**(`JapanTime.Offset`)。JST に夏時間は無く、
  タイムゾーン DB を引かないぶんコンテナに tzdata が無くても壊れない
- 書式は `Format`(`2026-08-10 21:30 JST`)/ `FormatShort`(`8/10 21:30 JST`)/
  `FormatDate` / `FormatCompact`(ゾーンの字を落とす。何百行と並ぶ表の列用)/
  `FormatStamp`(ファイル名)。**`CultureInfo.InvariantCulture` で書式化する** ——
  和暦の環境で同じ画面の日時が揃わなくなるため
- **画面以外にも効く。** ntfy の通知タイトル、**LLM への入力**(`DigestPrompt` の
  イベント日時。LLM は渡した時刻を本文に書き写すので、UTC のまま渡すと読者と 9 時間ずれる)、
  語彙の持ち出しファイル名も JST
- **「今日」の境界も JST で数える。** Doorkeeper の `since` は開催地の日付で問い合わせる
  (UTC の日付だと日本の朝 9 時までは前日を指す)
- **DB へ渡す直前に UTC へそろえるのは `TechAntennaDbContext` の値変換1か所。**
  Npgsql は `timestamp with time zone` に**時差 0 以外の `DateTimeOffset` を書けない**
  (`Cannot write DateTimeOffset with Offset=09:00:00 ...`)。収集元は時差を付けたまま
  返してくる(connpass の `started_at` は `+09:00`)ので、**保存も問い合わせの
  パラメータも**モデル側の値変換で UTC にする。列は `timestamptz`(= 時点)なので、
  時差そのものは元から保存されない。
  収集元ごとに `ToUniversalTime()` を書いて回らないこと —— **書き忘れた1つが
  「その収集元だけ 1 件も保存されない」になる**(実際、記事のパーサだけが直していて
  connpass / Doorkeeper のイベントは保存できていなかった。カレンダーの月の境界を
  JST のまま問い合わせに渡して画面も落ちた)。
  **InMemory のストアでは再現しない**ので、`EfEventStoreTranslationTests` が
  DB につながずにモデルの値変換を見張っている

## 画面が本番でだけ落ちる2つの型

どちらも**手元(接続文字列なし = InMemory ストア)では起きない**。画面を足したら、
実際の PostgreSQL につないで開くところまで確かめること。

- **EF が翻訳できない LINQ。** InMemory は LINQ をそのまま実行するので通ってしまう。
  record のコンストラクタで射影してから、その列で並べ替えると落ちた
  (`OrganizerCountQuery` のコメント)。翻訳できるかは `ToQueryString` で
  **DB につながずに**確かめられる(`EfEventStoreTranslationTests`)
- **非同期の初期化中の1回目の描画。** `OnInitializedAsync` が同期で終わらないと、
  `ComponentBase` は待っているあいだに**一度描画する**。InMemory のストアは同期に
  完了するのでこの中間状態が現れないが、DB につなぐと現れる ——
  読み込み中に触る値を `default!` で「必ずある」ことにしていると、そこで
  `NullReferenceException` になり画面ごと 500 になる(イベントのカレンダーで踏んだ)。
  **読み込みで埋まるフィールドは null 許容のまま持ち、埋まるまでは「読み込み中…」を出す**
  (他の一覧ページと同じ作り)

## 見た目(CSS)

**Bootstrap は使っていない。** Blazor の雛形が入れた
`lib/bootstrap/dist/css/bootstrap.min.css` への `<link>` は残っていたが**実体が無く、
毎回 404 を引いていた** —— `list-unstyled` が効かず箇条書きの点が出ていたのはこのため。
link を外し、razor が実際に使っているユーティリティ(`text-muted`・`badge`・`table`・
`btn`・`ms-1` など 20 個ほど)だけを `wwwroot/app.css` に自前で持っている。
**クラス名は Bootstrap のものに合わせてある**ので razor 側は書き換えていない。
新しいユーティリティを razor で使うときは、app.css の「ユーティリティ」節に足すこと
(足さないと無言で効かない)。

色・余白・角丸・影は `app.css` の `:root` に**カスタムプロパティ**として置き、
各 `*.razor.css` はそれを参照する。**個々の CSS に生の色を書かないこと** ——
1か所で調整できなくなる。

- **ダークモードは `prefers-color-scheme` で OS に追従する**。全ページ静的 SSR なので
  切り替えボタン(JS と保存先が要る)は置かない。`:root` に `color-scheme: light dark`
  を宣言してあるので、チェックボックス等のフォーム部品も一緒に切り替わる
- サイドバーはライト/ダークどちらでも暗い面のまま(`--nav-*`)。雛形の紺→紫
  グラデーションはやめ、単色にしてある
- 一覧(記事・イベント・書籍・トピック詳細)は**同じ形のカード**で出す。種別ごとに
  見た目を変えると、3種を並べて見るページで追いにくくなる
- **プレースホルダーは入力済みの値と見間違えない濃さにする**(app.css の `::placeholder`)。
  ブラウザ既定は本文に近い濃さで、**設定済みの値が入っているように見える**
  (実際に「実行する時刻」が設定済みに見えた)
- **焦点の輪(`:focus-visible`)は操作できる要素にだけ付ける。** 全要素に付けると、
  `FocusOnNavigate`(`Routes.razor`)が読み上げの起点として h1 に `tabindex="-1"` を
  足して焦点を移すぶん、**ページを開くたび見出しが枠で囲まれて出る**(静的 SSR でも
  `blazor.web.js` が動くので起きる)。app.css は `[tabindex="-1"]` を除いている
- 和文の折り返しは `ch` ではなく `em` で測る。`ch` は "0" の字幅なので、全角が並ぶと
  **想定の半分の位置で折り返す**
- **`[hidden] { display: none !important }` を app.css に置いてある。** ブラウザ既定の
  `[hidden]` は作者スタイルに負けるので、`display` を持つクラス(`.btn` は `inline-flex`)が
  付いた要素は `hidden` を付けても出たままになる —— **「JS が動いたときだけ出す/隠す」作りが
  静かに壊れる**(実測: `.btn` + `hidden` の計算値が `inline-flex`。コピーのボタンは
  JS 無しでも出ていて、トピックの「選択を保存」もその場保存の効いている画面に残っていた)
- **PWA(ホーム画面から起動した状態)ではブラウザの戻る/進むが消える。** 同じ2つを
  サイドバー上部(ロゴの左)に置いてある。出すのは `wwwroot/history-nav.js` が
  `display-mode`(と iOS の `navigator.standalone`)を見て `html` に `.standalone` を
  付けたときだけ —— **タブで開いているときは出さない**(アドレスバーと二重になる)。
  **行き先が無くても無効化はしない** —— 履歴のどこにいるかを知る API が無く、
  `history.length` では判定できない(戻っても減らない)。状態を偽って出すよりよい

## PWA(インストールして使う)

`wwwroot/manifest.webmanifest` で**インストールできる**ようにしてある
(`display: standalone` / `start_url: /` / 192・512・maskable のアイコン)。
iOS は 16.4 より前がマニフェストを見ないので、`apple-mobile-web-app-capable` でも
同じことを言う(`App.razor`)。**テーマ色はサイドバー上端と同じ `#0f1116`**。

- **サービスワーカー(`wwwroot/service-worker.js`)はページもデータもキャッシュしない。**
  引き受けるのは「インストールの条件を満たすこと」と「通信できないときに
  `offline.html` を返すこと」だけ —— このアプリは集めた情報を毎回サーバーから読む画面で、
  古い HTML を返すと**昨日の一覧を今日のものとして見せる**ことになる(本人には見分けが
  付かない)。静的アセットも持たない(Blazor の指紋付き URL に HTTP キャッシュが効く)
- **横取りするのはページの移動(GET)だけ。** フォームの POST を触ると、保存の失敗を
  オフライン画面で覆い隠してしまう
- `offline.html` は**スタイルを自分の中に持つ**(app.css を読みに行くと、オフラインでは
  取れずに素の文字だけになる)
- **登録は安全なコンテキスト(https か localhost)でだけ**成立する。LAN の IP へ http で
  当てている場合は登録されず、インストールもできない(ブラウザ側の決まり。
  `register-sw.js` は静かに何もしない)
- 版を上げるときは `service-worker.js` の `VERSION` を進める(古いキャッシュは
  `activate` で消える)

## アイコン

ファビコンは**左上のロゴと同じアンテナ**(`Components/Layout/NavMenu.razor` の
`.brand-mark`)を、サイドバーと同じ紺〜紫のグラデーションの角丸プレートに載せたもの。

- `wwwroot/favicon.svg` — 対応ブラウザはこれを使う(拡大しても崩れない)
- `wwwroot/favicon.png`(32px) — fallback。`wwwroot/favicon.ico` は `<link>` を見ずに
  `/favicon.ico` を取りに来る相手向け(中身は同じ 32px の PNG)
- `wwwroot/apple-touch-icon.png`(180px) — iOS のホーム画面追加用。**角丸なしの全面塗り**で
  出す(角丸は OS 側がかけるので、こちらで丸めると二重に丸まって縁が痩せる)。
  ホーム画面での名前はページごとの `<title>` ではなく `apple-mobile-web-app-title` から取らせる
- `wwwroot/icon-192.png` / `icon-512.png` — PWA(マニフェスト)用。**インストールの条件が
  この 2 枚**なので、片方だけだとブラウザが「インストール可能」と見なさない
- `wwwroot/icon-maskable-512.png` — `purpose: maskable` 用。**OS が好きな形(円・角丸・雫)に
  切り抜く**ので、絵を中央 80% の安全域に収め(マークを 0.72 倍)、背景は隅まで塗る

PNG / ICO は `tools/generate-icons.py` で生成する。ImageMagick も PIL も要らないよう、
距離関数でアンチエイリアスをかけて自前でラスタライズし、zlib で PNG を組んでいる
(標準ライブラリのみ)。

```
python3 tools/generate-icons.py src/TechAntenna.Web/wwwroot
```

**形の定義が `favicon.svg` と `tools/generate-icons.py` の 2 か所にある。**
片方だけ変えると SVG と PNG で絵が食い違うので、必ず両方そろえて直すこと。
ロゴ自体(`NavMenu.razor`)を変えたときも同じ。

## 構成

- `src/TechAntenna.Core` — ドメインモデルと抽象。外部パッケージへの依存を持たない
- `src/TechAntenna.Infrastructure` — EF Core・外部 API クライアント等の実装(Core を参照)
- `src/TechAntenna.Web` — ASP.NET Core + Blazor (Server)。収集ジョブの `BackgroundService` もここでホストする
- `tests/TechAntenna.Tests` — xUnit

## DB

**テーブル定義は [docs/database.md](docs/database.md) にまとめてある**(列・型・意味・索引・
ER 図・トピック3テーブルの関係)。**DB に変更を入れたら、同じコミットでこの文書も更新すること**
—— README や CLAUDE.md と同じ扱いで、実装に追従させる対象。更新が必要になるのは、
`src/TechAntenna.Core/` のモデル・`TechAntennaDbContext` の設定・マイグレーションの
いずれかを触ったとき(列の追加/削除、意味の変更、索引、重複判定キー、テーブルの追加)。
**マイグレーションを積み上げるだけでは最終形が読めない**ので、この文書が最終形の側を持つ。

- PostgreSQL + EF Core。接続文字列は `ConnectionStrings:Default`(環境変数なら
  `ConnectionStrings__Default`)で渡す。**未設定ならメモリ上のストアにフォールバック**する
- マイグレーションは起動時に自動適用される(個人運用前提)。
  **本格稼働前に履歴は 1 本(`InitialCreate`)にまとめ直した** —— 16 個積み上がって
  最終形が読めなくなっていたため。以後は積み上げる
- **語彙(`Topics`)が空の起動では `topic-seed.json` が初期値として流し込まれる**
  (`TopicSeeder`。実測でトピック 83 件・タグ 245 件)。語彙がまったく無いと LLM が
  寄せ先も親も選べず、同義の親が二重にできるため
- マイグレーション追加:
  `dotnet ef migrations add <名前> -p src/TechAntenna.Infrastructure -s src/TechAntenna.Web`
- **compose の db は 7021 をホストへ公開しない**(本番の DB を外に出す理由が無いため)。
  ホストの開発サーバーから実データを読みたいときだけ、上書き定義
  `docker-compose.dev.yml` を重ねて `127.0.0.1:7021` に開ける(手順は README「開発環境」)。
  **開発サーバーも起動時にマイグレーションを自動適用する**ので、未コミットの
  マイグレーションを持ったまま本番の DB へ繋がないこと。SQL で覗くだけなら開ける必要は
  なく、`docker compose exec db psql -U techantenna -d techantenna` で足りる
- **`POSTGRES_PASSWORD` が効くのは `data/postgres` を作る初回起動のときだけ。** 後から
  変えても DB 側は変わらず、接続文字列だけがずれて起動しなくなる。compose には既定値を
  置いてあり、`.env`(standalone は 2 箇所の直書き)で上書きできる
- **テストは PostgreSQL を使わない**(`tests/` が触るのは `InMemory*Store` だけ)。
  DB を上げていなくても `dotnet test` は通る

## 本番の実行形態(Docker)

本番は「コンテナイメージを docker compose で動かす」形態。アプリの構成
(プロジェクト構成・必要な環境変数・待ち受けポート)を変えたら、同じコミットで
`Dockerfile`・`docker-compose.yml`・`.env.example` も追従させる。

イメージは **main への push で GitHub Actions がビルドし GHCR へ公開する**
(`.github/workflows/build-and-push-image.yml`)。タグは `latest` とコミット識別用の
`sha-xxxxxxx`、および `v*` タグを打ったときのバージョン。

**非公開リポジトリでは Actions の実行時間も GHCR のストレージ・転送量もプラン付属の枠を
消費する**(このイメージはベース層だけで圧縮後 90MB/アーキ)。枠を使いすぎないよう、
ワークフローには次の制限を掛けてある。外すときは枠の消費が増えることを承知して外すこと。

- **amd64 のみ**ビルドする。arm64 のネイティブランナーは公開リポジトリでないと無料枠で
  使えず、QEMU 上の `dotnet publish` は極端に遅い。arm64 が要るときは Dockerfile が
  クロスコンパイルに対応しているので手元で `docker buildx build --platform linux/arm64` する
- **`paths-ignore` で文書だけの変更を除外**する(`**.md`・`docker-compose*.yml`・
  `.env.example`)。イメージの中身が変わらない push で実行時間を使わないため
- **`concurrency` で同じ ref の古い実行を打ち切る**
- レイヤーキャッシュ(`type=gha`)を実行間で使い回す

- `Dockerfile` — マルチステージ。`sdk:10.0` で publish し、`aspnet:10.0` に成果物だけを載せて
  非 root(`USER $APP_UID`=1654)で `dotnet TechAntenna.Web.dll` を実行する。HTTP 7020 のみ待ち受け
  - **Claude Code の CLI は同梱しない**(以前は入れていて、イメージが約 370MB → 730MB に
    なっていた)。要約をサブスクの枠で回すときは別コンテナの CLI ブリッジへ HTTP で頼み、
    トークンは `/app/state` の設定 DB 経由で渡す(「Claude Code 方式」参照)
  - **Alpine ではなく Debian ベース**を使う。Alpine 版の .NET イメージは globalization
    invariant モードが既定で、日本語の日付・文字列の書式が効かない
  - arm64 向けは QEMU ではなく **.NET のクロスコンパイル**(`--platform=$BUILDPLATFORM` +
    `dotnet publish -a`)で出す。実行ステージに `RUN` を置かないのもエミュレーションを
    避けるため(鍵置き場のディレクトリはビルドステージで作って `COPY --chown` する)
- `docker-compose.yml` — 本番用(`app` + `db` + Claude Code の CLI を動かす `bridge` +
  起動前に一度だけ走る `init`)。この定義自体はビルドせず GHCR のイメージを
  参照する。設定は `.env` から環境変数で渡す。`docker-compose.build.yml` はその場でビルドする
  上書き定義(`:local` タグ)で、手元での動作確認や GHCR を使わない起動はこちらを重ねて使う
  - Postgres は `postgres:18-alpine`。**18 のイメージは PGDATA が
    `/var/lib/postgresql/18/docker`** で、マウントするのはその1段上の
    `/var/lib/postgresql`(17 以前と位置が違うので、マウント先を変えると初期化し直しになる)
  - **永続データはホストの `data/` へバインドする**(`data/postgres`・`data/keys`・
    CLI ブリッジと共有する `data/state`。`init` が後の2つを UID 1654 に chown する)。
    名前付きボリュームを使わないのは、`data/` を丸ごとコピーするだけでバックアップに
    なるようにするため —— Docker の中に置くと持ち出しに一手間かかる。`data/` は
    `.gitignore` 済み。standalone 側は `${...}` を解決できないので、雛形の先頭に
    ホストの絶対パスを書く欄(`x-postgres-dir` / `x-keys-dir`)を置いてある
  - **`init` サービスが `data/keys` を `chown` してから `app` を起動する**
    (`depends_on` の `service_completed_successfully`)。ホストに作られる
    ディレクトリはホストのユーザー所有になるが、`app` はイメージが持つ非 root
    (UID 1654)で動くのでそのままでは鍵を書けない。名前付きボリュームなら Docker が
    見てくれていた部分で、バインドにした代償がここに出る。
    db 側は不要 —— postgres のエントリポイントが root で起動して自分で所有者を揃える。
    `init` に**アプリのイメージではなく `alpine` を使う**のは、`app` を `:local` で
    ビルドする構成のときに GHCR を引きに行かせないため
- `docker-compose.standalone.example.yml` — **`.env` もシェルの環境変数も無い環境**向けの
  単体定義の雛形。管理画面に YAML を貼り付けて起動するタイプ(NAS のコンテナマネージャー等)
  では `${...}` を解決できないため、値を直接書いてある。**`docker-compose.yml` を変えたら
  こちらも追従させること** —— 値が直書きなぶん古くなりやすい。**書き換えが要るのは
  データの置き場だけ**で、そこは `/path/to/…` のプレースホルダーにしてある ——
  特定の NAS 製品の実パスを書くと、そのまま使えるように見えて他の環境で壊れる。
  **リポジトリに置くのは `.example` の付いた雛形だけ**で、実値を入れてコピーした
  `docker-compose.standalone.yml` は `.gitignore` してある(`.env.example` と `.env` の関係と
  同じ。この形式は値を直書きするので、追記した瞬間に秘密がコミット対象に入る)
- GHCR に置く場合の公開先は `ghcr.io/rtcode337/tech-antenna`。`latest` だけでなく
  `sha-xxxxxxx` も打つ(どのコミットが動いているか後から特定できるようにするため。
  手順は README「本番運用」)

`Program.cs` にコンテナ運用のための分岐が2つある。

- **Data Protection の鍵の永続化** — `DataProtection:KeysDirectory`(Docker では
  `DataProtection__KeysDirectory=/app/keys`)が設定されていればそこへ保存する。既定の
  一時領域だとコンテナを作り直すたびに鍵が変わり、発行済みの antiforgery トークンが無効になる。
  起動時に出る「No XML encryptor configured」の警告は、鍵ファイル自体の暗号化(証明書や
  KMS が要る)を設定していないためで、ホスト内のボリュームに 0600 で置く運用のため許容している
- **HTTPS 前提の設定の切り替え** — `UseHttpsRedirection`/`UseHsts` は HTTPS の待ち受けが
  分かるときだけ有効にする(`httpsConfigured`)。TLS をリバースプロキシで終端する構成では
  コンテナは HTTP しか持たず、リダイレクト先が決まらないまま警告だけが出るため

## 待ち受けポート

**このアプリのポートは 7020 番台に固める**(開発ポートを 7000 番台へ 10 刻みで割り当てる
運用に合わせたもの。7020〜7029 がこのアプリの枠)。

| 番号 | 用途 |
|---|---|
| 7020 | アプリ。`dotnet run`・コンテナ内・compose の公開ポート(`PORT` の既定)すべて同じ |
| 7021 | PostgreSQL。コンテナ内も `PGPORT` で 7021(`docker-compose.dev.yml` で開くときも同じ) |
| 7022 | `dotnet watch`(ホットリロード)。**LAN・VPN の他の端末からも見える** |
| 7023 | `https` プロファイルの HTTPS(開発でだけ使う) |

**`watch` プロファイルは `0.0.0.0` に bind する**(`Properties/launchSettings.json`)。
`localhost` にすると loopback だけを待ち受けるので、スマホや VPN 経由の端末からは
ポートが開いていても届かない(症状は接続できないだけで、ログには何も出ない)。
**`http` プロファイル(7020)は `localhost` のまま** —— ブラウザを自動で開く設定なので、
`0.0.0.0` の URL を渡すと開けない。7020 を他の端末へ見せたいときは
`dotnet run --project src/TechAntenna.Web -- --urls http://0.0.0.0:7020` で上書きする
(compose で動かす場合は Docker が公開するので、この指定は要らない)。

**ホスト側とコンテナ内の番号をそろえてある** —— compose の `"7020:7020"` を読むだけで
対応が分かるようにするため。アプリ側はベースイメージの既定(8080)を Dockerfile の
`ASPNETCORE_HTTP_PORTS=7020` で上書きし、DB 側は `PGPORT` で移している(`PGPORT` は
サーバーもクライアントも読むので、`pg_isready` や `psql` に `-p` を足す必要はない)。

同じホストで開発サーバーと本番コンテナを同時に上げることはできない(片方の `PORT` を
変える)。**`watch` プロファイルだけ 7022** なのは、本番同等のコンテナ(7020)を上げたまま
並べられるようにするため。**6000 番台は使わない** —— Chrome/Firefox が X11 用ポートとして
拒否し(`ERR_UNSAFE_PORT`)ブラウザから開けなくなるため。

## ホットリロード(開発中)

`dotnet watch` が `.razor` / `.razor.css` / `.cs` の変更を拾い、**プロセスを再起動せずに**
反映する。ブラウザ側も自動で更新される —— Development では ASP.NET Core が
`aspnetcore-browser-refresh.js` を注入するため(注入されるのは `Accept: text/html` の
リクエストだけなので、`curl` で確かめるときはヘッダを付ける)。

```
dotnet watch --project src/TechAntenna.Web --launch-profile watch
# → http://localhost:7022（他の端末からは http://<このホスト>:7022）
```

- **効く**: Razor のマークアップ、scoped CSS(`*.razor.css`)、メソッド本体の C#
- **効かない**: DI の登録・`Program.cs` の構成・型の追加といった rude edit。
  この場合は watch がビルドし直してアプリを再起動する(数秒かかる)
- **コンテナには効かない。** 動いているコンテナに反映するにはイメージを作り直す
  (`docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build`)
- 接続文字列を渡さなければ In-Memory ストアで起動するので **DB 無しで画面を触れる**
  (データは空。compose の db は 7021 をホストへ公開していないので、実データを見るならコンテナ側)
- **始める前に `free -h` を見る。** ビルドとコンテナを同時に走らせるとメモリを使い切り、
  swap の無い環境では OOM でホストごと巻き込まれる
- **`dotnet build` / `dotnet test` は watch を止めてから走らせ、止めたあとは
  アプリのプロセスが残っていないか確かめる**(`watch` を kill してもアプリ
  (`bin/Debug/net10.0/TechAntenna.Web`)は生き残ることがある)。動いたままビルドすると、
  そのプロセスが覚えている静的アセットの指紋と obj 側の中身が食い違う ——
  以前これで **ブラウザだけ CSS が全部消える**(gzip を要求すると空の応答が返り、
  curl では再現しない)状態になった。**Debug では事前圧縮を切ってある**
  (`CompressionEnabled`。理由は `TechAntenna.Web.csproj` のコメント)ので同じ壊れ方は
  しないが、内容がずれること自体は起きる

## コマンド

- ビルド: `dotnet build`
- テスト: `dotnet test`
- Web 起動: `dotnet run --project src/TechAntenna.Web`(http://localhost:7020)
- ホットリロード付きで起動:
  `dotnet watch --project src/TechAntenna.Web --launch-profile watch`(http://localhost:7022)
- 本番同等の起動(GHCR から pull): `docker compose pull && docker compose up -d`
- 本番同等の起動(手元でビルド):
  `docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build`
